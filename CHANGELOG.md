# Changelog

All notable changes to Splatpipe will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Fixed
- **Phase 11A Issue 4 (v0.8.0 UX bug-fix sprint): interp popover applies to multi-selection.** The V popover (`_gzOpenPopover` -> `_gzSetInterp`) wrote only to the gizmo-selected kf (`_gzSelKf`). A box-select of 3 kfs followed by V + click was a no-op for kfs 2 and 3. User report: "When I select several keyframes and change the interpolation, it doesn't seem to work for multiple keyframes. But it should." Fix in `template_parts/17_editor_gizmo.js_tmpl`:
  - `_gzSetInterp(val)` now reads `window.__editor.tlSelection` (the Phase 2D timeline selection surface, an Array<number> sorted asc) and when it has >=2 entries iterates over EVERY index, writing the chosen interp to each + pushes ONE `EditorModuleRegistry.pushUndo('interp-multi')` snapshot for the batch (R8 §4.2 anti-pattern: NEVER per-target). The multi-write branch takes precedence over the gizmo-target branch (a multi-select implies the user is operating on the batch).
  - `_gzOpenPopover()` now opens when EITHER a gizmo target OR a multi-selection >= 2 exists -- V on a box-select was a silent no-op before because the open path bailed on `_gzSelKf < 0`.
  - The bezier seeding (which lights up the gizmo's tangent handles) still runs only on the gizmo-attached kf -- a non-attached kf in a multi-select that flips to bezier gets the interp marker without the visual tangent handles (those are gizmo-anchored by design).
  Verified live in the harness on `_viewer_t18`: box-select [0,1,2] + V + "stepped" -> all three kfs `interp: 'stepped'`. Single-target still works: clear selection + gzSelect(1) + V + "linear" -> only kf 1 changed.

- **Phase 11A Issue 7 (v0.8.0 UX bug-fix sprint): Save button visible feedback.** In http save mode the POST was silent (user clicked Save, no visible response, no idea if it succeeded). The cli mode at least popped up the token card; http had nothing. Fix in `template_parts/17_editor_gizmo.js_tmpl::saveBtn` click handler: the button label + background now cycle Save -> Saving... -> OK Saved / Save failed -> Save (after 2 s). Driven by `_gzSave()`'s return value: cli mode (string token) flashes OK immediately, http mode (fetch Promise) flips OK on `response.ok`, Save failed on a non-ok status or fetch reject (with a `console.warn` of the status / network error for debug). Verified live in the harness: cli mode -> OK Saved (green), http mode with mocked fetch -> Saving... (gray) -> OK Saved (green); error case (500) -> Save failed (red).

- **Phase 11A Issue 6 (v0.8.0 UX bug-fix sprint): `total_duration_s` now actually extends the scrub range past the last keyframe.** The Phase 2C (#122 §3.7) timeline UI surfaced a `total-time` input that wrote `path.total_duration_s`, but only the Prev/Next-keyframe ceiling consulted it -- the `#path-scrub` slider still mapped to `_player.duration = last_kf_t + holds`, so typing "40" on a path whose last kf is at 25 s left the scrub stuck at 25 s. Fix in two places:
  - `template_parts/09_playback_spline.js_tmpl::buildPlayer` -- the player's `duration` is now `max(kfDuration, total_duration_s)` when the latter is a positive number > kfDuration. Past the last knot the spline `evaluate()` clamp already returns the last pose (09 line 30), so the extension is a "hold at end" segment -- safe + DCC-standard. For looped paths the wrap length lands at the extended duration so the loop includes the hold.
  - `template_parts/16_editor_timeline.js_tmpl::_tlDuration` -- mirror lift so the strip ruler + playhead<->scrub mapping align with the player. Without this mirror the timeline canvas would stop drawing at the last kf even though the player + scrub can reach the extended range.
  Verified live in the local harness: gzPath (kfs at 0/12/24 s) with `tlSetTotalDuration(40)` -> `tlDuration=40`, `tlEffectiveScrubRange=[0,40]`, scrub at 1000 -> `tlPlayheadT=40`.

- **Phase 11A Issues 1+2 (v0.8.0 UX bug-fix sprint): Perspective free-fly + trajectory overlay.** Two regressions in the live `?author=1` editor on kf-fehmarn reported via Telegram 2026-05-21 00:32:
  - **Issue 1 -- Perspective controls leaked-disabled.** After scrubbing the timeline (which sets `controls.enabled = false` via the `#path-scrub` `input` handler), then switching to Perspective via the top-bar dropdown, OrbitControls stayed dead because `_camSelApply`'s Perspective branch only re-enabled controls when `_player` was truthy (via the `stopPath()` it calls). A fresh scrub-then-perspective workflow could leave `_player` null while `controls.enabled` was still false. Fix in `template_parts/10_camera_select.js_tmpl::_camSelApply`: the Perspective branch now UNCONDITIONALLY sets `controls.enabled = true` and clears `_pausedAt` / `_pausedAtPlayer` so the per-frame `_trajLayer.update()` rebase no longer pins the camera at the last scrub frame.
  - **Issue 2 -- Trajectory disappeared in Perspective.** The author-mode fallback in `_trajActivePath` walked `selEl.value` -> `cfg.default_path_id` -> first cameraPath. After "+ Add camera" the new empty camera (0 keyframes) became BOTH the selEl value AND `cfg.default_path_id`, so the fallback returned the empty path and `_trajRebuild` drew nothing. Fix in `template_parts/15_editor_trajectory.js_tmpl::_trajActivePath`: prefer paths with >=2 keyframes (renderable) in the fallback walk so the Fehmarn Cinematic trajectory still shows in Perspective when the current bind is a fresh empty camera. Original fallback chain is preserved as the no-renderable-path safety net.
  - Test surface: re-pinned the output-pin lockstep (all 6 fixtures shifted by +2720 bytes uniformly; verified live in the harness on `_viewer_t18` -- scrub+Perspective gives `controls.enabled=true`, +Add camera + Perspective gives `groupVisible=true, activePathId='gzPath', frustumCount=3`).

## [0.8.0] - 2026-05-21

### Added
- **Phase 7 (editor-arc-design #122 §4.4): `AudioModule` -- track CRUD + edit-time UI (per user-locked Q3: build the editor capacity now, user provides assets later).** New fragment `src/splatpipe/viewers/spark/template_parts/15f_audio_module.js_tmpl` registers the canonical owner of the `cfg.audio` slot with the Phase-2 `EditorModuleRegistry`. The schema (already present in `PUBLIC_VIEWER_CONFIG_KEYS`) is `[{file, positional, volume, loop, pos?}, ...]`; `cfg.audio` defaults to `[]` (empty array -- no audio) so editing a fresh scene is a clean slate. Contract: `name='audio'`, `stateKey='audio'`, `defaultModes=['author','user','embed']` (audio plays in EVERY mode -- it is a public scene element, not an editor-only feature; the EDITOR UI is gated separately by the Scene Settings drawer's HudLayer registration in 17b). `mount(overlay, hud, interaction, modes)` builds an `HTMLAudioElement` per `cfg.audio` entry + auto-plays each track (matches the legacy 08_input loader behaviour; `play()` returns a promise that may reject under autoplay policy -- silent-warns rather than throws). `unmount()` pauses every element + clears its `src` (releases the underlying network connection + media buffer immediately). `onCfgChange` rebuilds from the new cfg (an EditHistory undo, or a post-save reload). `getDirtyState`/`markClean` use the two-track signal (explicit `_isDirty` flag + conservative slice-signature diff for post-undo paths), mirroring the 15b PanoramaModule pattern. WHY `HTMLAudioElement` (NOT `THREE.Audio`): v1 ships the capacity to manage tracks; positional audio via Web Audio API + `PositionalAudio` is documented in the schema but DEFERRED to Phase 9 -- the "positional" toggle still stores the flag so the schema is forward-compatible, but the module plays positional tracks as plain non-positional audio in v1 (HTMLAudioElement is simpler than Web Audio context-resume gymnastics). Edit ops (each pushes ONE `EditorModuleRegistry.pushUndo` snapshot per gesture, labels: `audio-add` / `audio-delete` / `audio-volume` / `audio-loop` / `audio-positional` / `audio-pos`): `_addTrack(file, opts)` (pushes a new track with sensible defaults: volume=0.5, loop=true, positional=false), `_deleteTrack(idx)` (splices the track + tears down its element), `_updateTrack(idx, patch)` (selectively updates `volume`/`loop`/`positional`/`pos`; live-applies volume + loop to the running element so the author hears it immediately, no rebuild needed). `renderSceneSettings(parentEl)` populates the drawer's Audio section with per-track cards (volume slider gesture-end snapshot pattern -- pushUndo on `pointerdown`, commit on `change`/`blur`, NEVER per `input` -- the R8 §4.2 anti-pattern; loop + positional checkbox toggles; delete button with confirm) + a "+ Add track" button that wires a hidden `<input type="file">` to a FormData POST to `../upload-audio` (the bug-audit-#7 hardened route; relative URL resolves to `/projects/<path>/upload-audio` from the dashboard preview). On upload success the returned `data.path` is added as a new track via `_addTrack`. The drawer self-attach uses `setTimeout(_attachToDrawer, 0)` from `mount()` so the placeholder section 17b emits is replaced cleanly regardless of fragment order (mirrors the 15b PanoramaModule pattern). `timelineLane: {rows:1, label:'audio', render(ctx,x,y,w,h,t)}` contributes continuous per-track full-width blocks (audio is a background loop in v1; per-track `t_in`/`t_out` is deferred -- the schema in PUBLIC_VIEWER_CONFIG_KEYS has no t_in/t_out fields in v1). Each track gets a horizontal strip stacked inside the lane height, coloured from a cyclic 6-colour palette so each track is visually distinct. Tiny guard added to `08_input.js_tmpl`: the legacy `THREE.AudioListener` + `THREE.AudioLoader` block (which the AudioModule supersedes) is now wrapped in `if (!_audioOwnedByModule)` -- defensive against a future load-order change; the guard checks `window.__sceneview.modules.get('audio')` (same pattern as Phase 5's `_annotationsOwnedByModule`). `core/config_merge.py::ALLOWED_PATCH_KEYS` gains `audio` (whole-replace semantics -- the patch wholesale-replaces the entire `audio: []` array; no per-track merge); the cross-language twins `infra/cloudflare/worker.js` and `infra/php/save-camera.php` updated in lockstep so the cross-language oracles still pass. Test surface on `window.__editor.{audioGet, audioModuleDirty, audioElements, audioTrackCount, audioSectionEl, audioAddTrack, audioDeleteTrack, audioUpdateTrack, audioRender}` -- every entry is a function (not a getter) so it sidesteps the registry's `Object.assign` getter-freeze pattern (mirrors 15d/CutsModule). Locked by 11 new tests in `tests/test_audio_module.py`: fragment file BOM/CRLF hygiene + AudioModule + register markers, full EditorModule contract surface (12 markers covering name/stateKey/defaultModes/mount/unmount/getDirtyState/markClean/onCfgChange/renderSceneSettings + EditHistory snapshot labels + HTMLAudioElement primitive + upload route + "+ Add track" + testSurface), fragment lexical-ordering invariant (15f after 15a-15d, before 18_frame_loop), 08_input guard structure (declaration form + lookup uses `window.__sceneview.modules.get('audio')` + legacy THREE.AudioListener block stays inside the guarded scope), Python-side allow-list (audio in ALLOWED_PATCH_KEYS, whole-replace semantics, primary_asset force-keep invariant when audio rides with hostile primary_asset, empty-array patch clears existing tracks), 17b placeholder section presence. No edit to `06_cfg.js_tmpl` / `07_setup_three_spark.js_tmpl` / `11_clip_player.js_tmpl` / `15a-e_*.js_tmpl` (other-phase territory); the `08_input.js_tmpl` edit is the small Phase-5-pattern guard alongside the existing annotation guard.

- **Phase 8 (editor-arc-design #122 §4.7): `TitlesModule` -- CSS2D billboard renderer + edit-time UI for `titles3d`.** New fragment `src/splatpipe/viewers/spark/template_parts/15g_titles_module.js_tmpl` registers the canonical owner of the `cfg.titles3d` slot with the Phase-2 `EditorModuleRegistry`. `titles3d` has lived in `core/config_safety.PUBLIC_VIEWER_CONFIG_KEYS` + `core/config_merge.ALLOWED_PATCH_KEYS` for the whole Phase-2 rollout; the gizmo's legacy `_PATCH_KEYS` walk has been bouncing it through every save UNCHANGED -- but no renderer + no editor existed before this commit. Phase 8 ships BOTH: a CSS2D billboard per `cfg.titles3d[i]` (text + position + size + color) that fades in/out with `fade_ms` smoothstep edges across the `[t_in, t_out]` timeline window AND a Scene Settings drawer section + multi-lane timeline bar. Contract: `name='titles3d'`, `stateKey='titles3d'`, `defaultModes=['author','user','embed']` (the labels render in EVERY mode -- they are a public scene element, not an editor-only feature; the EDITOR UI is gated separately by the Scene Settings drawer's HudLayer registration in 17b). RENDERER CHOICE -- CSS2D over CSS3D: per spec §4.7 ("CSS3DObject or sprite + canvas-texture as fallback") v1 ships the CSS2D path because billboard semantics (always face camera) are built into CSS2DObject + the existing `css2d` CSS2DRenderer (mounted by 08_input) is ALREADY rendering each frame via `css2d.render(scene, camera)` in 18_frame_loop -- no second renderer, no second DOM container, no second rAF, no dynamic-import dance, no edit to the contested 07_setup_three_spark fragment. AnnotationModule (15c) proved the pattern for dot markers; TitlesModule reuses the same proven pipeline. `billboard:false` (a flat 3D-rotated CSS3DObject per `quat`) is documented as a future extension; for v1 the renderer EXPLICITLY skips an entry with `billboard === false` (`if (t3d.billboard === false) continue;` in `_buildFromCfg`) so a hand-authored CSS3D-expecting entry does NOT silently render as a CSS2D billboard (which would be a UI lie). `mount(overlay, hud, interaction, modes)` builds CSS2DObject children parented under the IIFE-scope `scene` (from 07_setup_three_spark) + registers an OverlayScene tick layer (`modes:['author','user','embed']`) that fades each label per `_timelineOpacity(t3d, _playheadTime())` each frame; outside the `[t_in, t_out]` window the label is fully hidden (`visibility:hidden` + opacity=0) so the cinematic "appear/disappear at keyframe" effect is exact. `unmount()` walks every record + removes the CSS2DObject from its scene parent + removes the DOM element via `parentNode.removeChild`. `onCfgChange` (wholesale cfg replacement, e.g. EditHistory undo) rebuilds if the slice signature changed. `getDirtyState`/`markClean` follow the conservative JSON-stringify slice-diff pattern (mirrors 15a/15c). The JS-side `TITLE_DEFAULTS` literal -- `text=''`, `pos=[0,0,0]`, `billboard=true`, `size=24`, `color='#ffffff'`, `t_in=0`, `t_out=999`, `fade_ms=300` -- is the v1 source of truth (titles3d had no Python-side `TITLE_DEFAULTS` constant before this phase because it was rendered-out); a future `core/path_io.TITLE_DEFAULTS` constant can be cross-checked against this list via a Node round-trip test, mirroring the 15c annotation-defaults parity check. `_upgradeTitle(t)` is the JS legacy-tolerant read helper that fills missing fields at READ time (input is NEVER mutated; output is a fresh shallow copy + defaults); auto-generates a `t3d_<hex>` id via `_newTitleId()` (mirrors `core/path_io._new_annotation_id` -- 4 random bytes -> 8 hex chars; Math.random fallback when `window.crypto` is shimmed away in a Node test rig). Per-frame tick: ONE `_timelineOpacity` lookup per title (<=16 titles per scene = sub-microsecond). `textContent` is assigned (NEVER `innerHTML`) so a hand-authored `cfg.titles3d[i].text` cannot inject markup. `timelineLane: {rows:1, label:'titles', render(ctx,x,y,w,h,tCur)}` -- per spec §4.10 "thin white bars" -- draws ONE white horizontal bar per title mapped from `[t_in..t_out]` to the lane's pixel rect via the master playhead's `_player.duration`; the multi-lane registry in 16_editor_timeline pulls it. `renderSceneSettings(parentEl)`: titles3d is NOT in 17b's `_SS_PLACEHOLDER_ORDER` (the six pre-listed placeholders cover Phase 3/4/5/7/2 sections) -- Phase 8 CREATES a fresh `<details data-section="titles3d">` + appends it, mirroring the Phase-6 CutsModule pattern exactly (17b is Phase 2D-owned and MUST NOT grow new placeholders here). The section contains a "+ Add title" button at the TOP (places at `controls.target` -- the orbit pivot -- else `camera.position + forward` as a mobile-no-orbit fallback; mirrors the 15c annotations section placement) + an accordion of per-title rows: text input (commit on `change`/`Enter`, NEVER per-keystroke -- R8 §4.2), color picker (`<input type="color">`), size slider (8..96px, gesture-START on `pointerdown`, commit on `change`), compact `t_in`/`t_out`/`fade_ms` numeric inputs (commit on `change`/`Enter`), and a delete button. Every CRUD pushes ONE `EditorModuleRegistry.pushUndo` snapshot per gesture (labels: `title-add` / `title-delete` / `title-text` / `title-color` / `title-size` / `title-t-in` / `title-t-out` / `title-fade-ms` -- ONE per gesture, NEVER per drag-frame). Test surface on `window.__editor.{titlesGet, titlesModuleDirty, titlesCount, titlesIds, titlesGetById, titlesObj3DById, titlesElById, titlesIsVisible, titlesForceTick, titlesLane, titlesSectionEl, titlesAddAt, titlesDelete, titlesRender, _upgradeTitle, _timelineOpacity, _TITLE_DEFAULTS}` -- every entry is a FUNCTION (not a getter) so it sidesteps the registry's `Object.assign` getter-freeze pattern (documented in 16_editor_timeline + 15d). Locked by 13 new tests in `tests/test_titles_module.py`: 8 STATIC (rendered-HTML markers for the IIFE / contract fields / CSS2DObject reuse / OverlayScene tick / timeline lane / Scene Settings hook + EditHistory snapshot labels + testSurface; fragment file BOM/CRLF hygiene; fragment lexical-ordering invariant 15a < 15g < 18_frame_loop; "does not touch contested fragments" guard that proves zero references to `TitlesModule`/`_titlesInit`/`titlesModule` in 06_cfg/07_setup/08_input/11_clip_player/17a/17b; OverlayScene tick + defaultModes both ['author','user','embed']; timeline lane uses `_player.duration` + `t3d.t_in`/`t3d.t_out` + white `fillStyle`; drawer section NOT in 17b's placeholder order + creates fresh section + idempotency check; v1 supports billboard=true only with the explicit `billboard === false` continue) + 5 DYNAMIC Node-driven (JS TITLE_DEFAULTS matches the documented v1 schema, `_upgradeTitle` fills defaults for a legacy 2-field entry with `t3d_<hex>` id auto-generation, `_upgradeTitle` preserves existing fields verbatim when set, `_timelineOpacity` smoothsteps in/out across the fade window with `1` at the centre of the window, fade_ms=0 yields a clean step function with no NaN/division-by-zero). 0 modifications to `06_cfg.js_tmpl` / `07_setup_three_spark.js_tmpl` / `08_input.js_tmpl` / `11_clip_player.js_tmpl` / `15a/b/c/d_*.js_tmpl` / `17a_edit_history.js_tmpl` / `17b_scene_settings_drawer.js_tmpl` (other-phase territory). Schema lock already shipped in `core/config_safety.PUBLIC_VIEWER_CONFIG_KEYS` + `core/config_merge.ALLOWED_PATCH_KEYS`; this commit ships the JS-side renderer + editor that finally USES it.

- **Phase 4 (editor-arc-design #122 §4.X): `PostFXModule` -- tonemap preset + exposure slider in Scene Settings drawer.** New fragment `src/splatpipe/viewers/spark/template_parts/15e_postfx_module.js_tmpl` registers the canonical owner of the `cfg.postprocessing` slot with the Phase-2 `EditorModuleRegistry`. The underlying `cfg.postprocessing` -> `renderer.toneMapping` + `renderer.toneMappingExposure` pipeline is ALREADY wired in `07_setup_three_spark.js_tmpl` (lines ~108-116 -- the `_TONEMAP` dict + the two `renderer.*` assignments); Phase 4 only adds the EDIT-TIME control surface that lets the author retune those values at runtime. Per R6 the module owns its own THREE.js setup via `mount()` (NO edit to `07_setup_three_spark.js_tmpl` -- modules self-contain). Contract: `name='postfx'`, `stateKey='postprocessing'`, `defaultModes=['author','user','embed']` (the renderer wiring applies in EVERY mode -- toneMapping runs on the framebuffer composite; the editor UI is gated separately by the Scene Settings drawer's HudLayer registration in 17b with `modes:['author']`), `mount(overlay, hud, interaction, modes)` captures the IIFE-scoped `renderer` (THREE.WebGLRenderer from 07) + re-applies `cfg.postprocessing` (defensive -- the boot-time wiring covers the cold-load case, but a wholesale-cfg replacement through `onCfgChange` must still produce correct pixels), `unmount()` is a no-op (toneMapping is a primitive int/float field, not a resource to dispose), `onCfgChange` re-applies the cfg + re-renders the drawer controls (handles undo/redo wholesale-cfg replacements cleanly), `getDirtyState` (two-track signal -- explicit `_isDirty` flag flipped by every gesture-end commit AND a conservative JSON-stringify slice-signature diff for post-undo paths, mirrors 15b PanoramaModule), `markClean` (clears flag + snapshots current slice as new baseline), `renderSceneSettings(parentEl)` (populates the Scene Settings drawer's "Post-FX" placeholder section), `timelineLane=null` (post-fx is scene-global, NOT animatable in v1 -- the registry filters `null`/missing so 16's `_tlLaneRegister` silently skips this module). THREE integration: the `_TONEMAP` dict is redeclared inside the module (the one in 07_setup_three_spark lives inside that fragment's IIFE closure -- no JS surface to reach it from 15e; redeclaring the same five entries keeps the module self-contained per R6 and ensures the runtime apply is byte-identical to 07's initial wiring). Five tonemap presets accepted (`linear` -> `THREE.LinearToneMapping`, `neutral` -> `THREE.NeutralToneMapping ?? THREE.NoToneMapping`, `aces` + `aces2` -> `THREE.ACESFilmicToneMapping`, `filmic` -> `THREE.AgXToneMapping ?? THREE.ACESFilmicToneMapping`). Editor UI (drawer section): tonemap preset dropdown (`<select class="pfx-tm">` with 5 `<option value="...">` entries: Linear / Neutral / ACES / ACES 2 / AgX (filmic)), exposure slider (`<input type="range" min="0.5" max="4" step="0.05" class="pfx-ex">`, default 1.5 from `cfg.postprocessing.exposure`), and a tooltip per spec §4.X + R7: "Lighting affects the panorama background only. Splats are pre-lit from training." Every UI control pushes ONE `EditorModuleRegistry.pushUndo` snapshot per gesture (labels: `postfx-tonemap` (commit-on-change, one click = one gesture) / `postfx-exposure` (gesture-START on slider `pointerdown` -- NEVER per `input` frame during a drag, the R8 §4.2 anti-pattern that would yield 60 entries/s)). Slider `input` handler updates the live preview ONLY (re-applies `_renderer.toneMappingExposure` directly without writing cfg); slider `change`/`blur` commits the value to `cfg.postprocessing.exposure` + emits `notifyCfgChange`. Drawer self-attach: 15e registers BEFORE 17b wires its `register` listener (fragment-order 15e < 17b), so the registry's optional `renderSceneSettings` hook never fires for this module via 17b's listener; the module SELF-attaches via `setTimeout(_attachToDrawer, 0)` from `mount()` -- the microtask fires AFTER every synchronous IIFE (including 17b) has finished, finds the placeholder `<details data-section="postfx">` 17b built (one of the six placeholders in spec §12.2 order), and replaces its body with the real controls (mirrors the 15b PanoramaModule pattern). `core/config_merge.py` `ALLOWED_PATCH_KEYS` extended with `postprocessing` so the editor save path (`merge_camera_scope` -> server-side merge) accepts a `postprocessing` patch slot (whole-replace semantics; the locked `primary_asset` force-keep invariant still holds when postprocessing rides in the same patch). `core/config_safety.PUBLIC_VIEWER_CONFIG_KEYS` already includes `postprocessing` (verified -- no #122 change needed; the publish_scene sanitiser passes it through to the public viewer-config.json). Test surface on `window.__editor.{postfxModule, postfxRenderer, postfxSectionEl, postfxDirty}`. Locked by 15 new tests in `tests/test_postfx_module.py` covering fragment file hygiene (BOM/CRLF), `cfg.postprocessing` default `{tonemapping: 'neutral', exposure: 1.5}` in 06_cfg, full EditorModule contract surface (name/stateKey/defaultModes/mount/unmount/getDirtyState/markClean/renderSceneSettings, registration with the coordinator), R6 modular-THREE-setup invariant (canonical `if (typeof renderer !== 'undefined' && renderer) _renderer = renderer;` IIFE-scope capture in 15e AND negative-control that `07_setup_three_spark.js_tmpl` carries NO PostFXModule reference while still wiring `renderer.toneMapping` + `renderer.toneMappingExposure`), THREE integration markers (the five tonemap key->THREE-constant mappings + the two `_renderer.*` field writes + the `THREE.NeutralToneMapping` default fallback), editor UI markers (`pfx-controls` / `pfx-tm` / `pfx-ex` / `pfx-ex-val` class names + all 5 dropdown option values + slider range), default-exposure-1.5 invariant, gesture-end snapshot push (both labels + `pushUndo` + slider `pointerdown` binding + negative-control that `pushUndo` is NEVER inside an `input` handler), Scene Settings drawer attachment (placeholder section selector + body class + the `setTimeout(_attachToDrawer, 0)` workaround), §4.X tooltip (the verbatim two-sentence copy), test-surface presence, no-timeline-lane invariant, Python-side `postprocessing` in `ALLOWED_PATCH_KEYS` + whole-replace contract + primary_asset force-keep when riding in the same patch, and `postprocessing` confirmed in `PUBLIC_VIEWER_CONFIG_KEYS`. No edit to `07_setup_three_spark.js_tmpl` / `08_input.js_tmpl` / `11_clip_player.js_tmpl` / `14_splat_budget.js_tmpl` / `15a_camera_path_module.js_tmpl` / `15b_panorama_module.js_tmpl` / `15c_annotation_module.js_tmpl` / `15d_cuts_module.js_tmpl` / `17a_edit_history.js_tmpl` / `17b_scene_settings_drawer.js_tmpl` (other-phase territory).

- **Phase 5 (editor-arc-design #122 §4.2 + §11.6): `AnnotationModule` -- dot_unfold + timeline-animatable + distance-based interactive annotations.** New fragment `src/splatpipe/viewers/spark/template_parts/15c_annotation_module.js_tmpl` registers the canonical owner of the `cfg.annotations` slot with the Phase-2 `EditorModuleRegistry`. Per user-locked Q6 (2026-05-20 17:38 voice): annotations are BOTH timeline-animatable (`t_in`/`t_out`/`fade_ms` opacity window) AND distance-based interactive (a small dot is always visible in 3D world space while inside the timeline window; when the camera enters `unfold_radius_m` OR the user clicks the dot, it expands into a full title + text + (optional) media panel). Contract: `name='annotations'`, `stateKey='annotations'`, `defaultModes=['author','user','embed']` (the BACKDROP/markers render in EVERY mode; the editor UI is gated separately by the Scene Settings drawer's HudLayer registration), `mount(overlay, hud, interaction, modes)` tears down the legacy 08_input `markerObjs[]` DOM (the AnnotationModule is the SOLE owner of annotation rendering from this point on; legacy consumers in 10_camera_select + 18_frame_loop walk the now-empty array length-tolerantly -- no-op), builds the dot + (hidden) panel DOM for every upgraded `cfg.annotations[i]`, registers an OverlayScene tick layer (active in all modes), `getDirtyState`/`markClean` follow the conservative JSON-stringify slice-diff pattern (mirrors 15a). The JS-side schema mirror -- `ANNOTATION_DEFAULTS` + `_upgradeAnnotation()` + `_newAnnotationId()` -- is byte-runtime-checked against the Python source of truth shipped in 9fa1405 (`core/path_io.ANNOTATION_DEFAULTS`). Per-frame tick: ONE Euclidean distance + ONE timeline-opacity lookup per annotation (<=16 per scene = sub-microsecond). Outside the `[t_in, t_out]` window the dot is hidden (`visibility:hidden` + `pointer-events:none` + panel `display:none` + force-fold any manual override) so the cinematic "appear/disappear at keyframe" effect is exact. Click handler on the dot DIV toggles a manual unfold override; the override clears when the camera EXITS the radius (per spec §4.2 -- "click overrides distance UNTIL camera moves away"). The cinematic path-active glow (`.ann-dot.path-active` class) from the existing `_lastTriggeredAnnotation` machinery (set by the 09_playback_spline annotation-trigger evaluator) is preserved by the AnnotationModule's tick reading the closure-scoped value -- so a tour's annotation highlight still pulses, even though the legacy markerObjs are gone. Timeline lane (`{rows:1, label:'ann', render(ctx,x,y,w,h,t)}`) draws ONE coloured bar per annotation mapped from `[t_in..t_out]` to the lane's pixel rect via the master playhead's `_player.duration`; cyclic 6-colour palette so each annotation gets a stable distinct bar colour. Scene Settings drawer section (`renderSceneSettings(parentEl)`): replaces the 17b placeholder via `querySelector('details[data-section="annotations"]')` + `removeChild`, then appends a fresh `<details>` with a "+ Add annotation" button at the top (places at `controls.target` -- the orbit pivot -- else `camera.position + forward` as a mobile-no-orbit fallback) plus an accordion of per-annotation rows: `kind` dropdown (`dot_unfold` / `title3d_overlay`), `unfold_radius_m` slider (0.5..50m), compact `t_in`/`t_out`/`fade_ms` numeric inputs (commit on `change` + `Enter` -- NEVER per `input`), and a delete button. Every CRUD pushes ONE `EditorModuleRegistry.pushUndo` snapshot per gesture (labels: `ann-add` / `ann-delete` / `ann-set-kind` / `ann-radius` / `ann-t-in` / `ann-t-out` / `ann-fade-ms`; slider gesture-START on `pointerdown` -- NEVER per drag-frame, R8 §4.2). Tiny guard added to `08_input.js_tmpl`: the legacy `for (const a of annotationsData)` block is now wrapped in `if (!_annotationsOwnedByModule)` -- defensive against a future load-order change where 15c runs before 08; the guard is value-false at 08's current runtime so the legacy code still runs once and the module's `mount()` tears it down (idempotent, same final state). Test surface on `window.__editor.annotations` exposes `{count, ids(), get(id), dotEl(id), panelEl(id), isUnfolded(id), toggleUnfold(id), forceTick(t), timelineLane, settingsEl, addAt(pos), _upgradeAnnotation, _timelineOpacity, _ANNOTATION_DEFAULTS}`. Locked by 12 new tests in `tests/test_annotation_module.py`: 7 STATIC (markers / fragment file hygiene / fragment-ordering invariant 15c < 18_frame_loop / drawer section placeholder replacement / 08_input guard structure / OverlayScene tick all-modes filter / timeline lane render signature) + 5 DYNAMIC Node-driven (JS ANNOTATION_DEFAULTS byte-matches Python source of truth, _upgradeAnnotation fills defaults for legacy 4-field entries + auto-generates `ann_<hex>` id, kind=title3d_overlay preserved when set, _timelineOpacity smoothsteps in/out across the fade window with `1` at the centre of the window, fade_ms=0 yields a clean step function with no NaN/division-by-zero). Schema shipped in 9fa1405; this commit ships the JS-side renderer + editor that consumes it. No edit to `06_cfg.js_tmpl` / `07_setup_three_spark.js_tmpl` / `11_clip_player.js_tmpl` / `15a_camera_path_module.js_tmpl` / `17a_edit_history.js_tmpl` / `17b_scene_settings_drawer.js_tmpl` (other-phase territory).

- **Phase 6 (editor-arc-design #122 §4.3): `CutsModule` thin wrapper around the existing ClipPlayer + edit-time NLE authoring surface (Scene Settings cuts CRUD + timeline lane).** New fragment `src/splatpipe/viewers/spark/template_parts/15d_cuts_module.js_tmpl` registers the existing multi-camera Camera-Cuts tour playback (the `11_clip_player.js_tmpl` IIFE that owns end-user playback) with the EditorModuleRegistry as the canonical owner of the `clips` stateKey. The module is duck-typed against the Phase-2 EditorModule contract: `name='cuts'`, `stateKey='clips'`, `defaultModes=['author','user','embed']` (clips drive playback in ALL modes; editing is author-only and gated separately inside renderSceneSettings), `mount`/`unmount` (no-op -- the 11_clip_player IIFE owns the playback machinery; we snapshot the boot cfg's clips slice as the clean baseline so getDirtyState reports false until the author makes their first edit), `getDirtyState` (returns `{isDirty, patchObject}` via a JSON-stringify diff vs the baseline OR the `_isDirty` flag set on every CRUD; conservative -- a false-positive only adds an extra slot to the SPCP1 payload), `markClean` (snapshots the current clips slice as the new baseline + clears `_isDirty`), `onCfgChange` (re-renders the cuts section + marks dirty for a wholesale cfg replacement), `timelineLane` (`{rows:1, label:'Cuts', render(ctx,x,y,w,h,playhead)}` -- per spec §4.10 multi-lane registry; draws coloured per-camera clip blocks scaled to lane width, with the camera_id label inside if block width allows), `renderSceneSettings(parentEl)` (populates the Scene Settings drawer's cuts section with per-clip cards: camera dropdown bound to `cfg.cameras`, in-point + duration numeric inputs that commit on blur/Enter, delete button with confirm, up/down reorder arrows, "+ Add clip" button disabled when no cameras exist). The 15d fragment loads AFTER 11_clip_player (whose IIFE-scope globals are referenced via closure-scope from the shared `<script type="module">` block -- the CutsModule is a THIN wrapper, NOT a re-implementation of playback) and AFTER 15a_camera_path_module (so the registry's section/lane registration order per spec §4.9 is preserved: CameraPathModule first, then CutsModule). Edit ops: `_addClip` (appends a new clip at the end of the sequence with auto-assigned `clip_start = sum(preceding durations)` for gap-free timeline; default duration=5.0s, in=0.0), `_deleteClip` (ripple-delete semantics -- re-packs `clip_start` so the remaining sequence stays contiguous, NOT leaving a hole), `_reorderClipUp`/`_reorderClipDown` (swaps `clip_start` with the neighbouring clip then re-packs; durations are preserved -- only playback positions change), `_retimeClip` (updates `duration` / `in` / `camera_id` in place + re-packs subsequent clip_starts if duration changed; invalid values rejected silently with input revert). Every CRUD pushes ONE `EditorModuleRegistry.pushUndo` snapshot per gesture (labels: `cuts-add` / `cuts-delete` / `cuts-reorder` / `cuts-retime`; mirrors the R8 §4.2 + Phase 2C convention -- never per keystroke). Stable per-camera colour palette (`_CUT_PALETTE` with 8 entries; djb2 hash so the same camera_id always picks the same colour) used by both the Scene Settings card swatch and the timeline-lane block fill. Annotation-trigger semantics across cut boundaries (Q6 per spec §11.6, one-shot per playback session) documented as a Phase 5/6 integration TODO -- the full unwind (annotation t_out past the clip boundary dropping cleanly into the next cut) is OUT OF SCOPE for this commit and lands when AnnotationModule wires its onClipBoundary listener. Test surface on `window.__editor.{cutsGet, cutsModuleDirty, cutsAddClip, cutsDeleteClip, cutsReorderUp, cutsReorderDown, cutsRetimeClip, cutsRender, cutsSectionEl, cutsCameraColor}` -- every entry is a FUNCTION (not a getter) so it sidesteps the registry's `Object.assign` getter-freeze pattern (documented in 16_editor_timeline at its `Object.defineProperties` site). 11_clip_player.js_tmpl is UNTOUCHED -- the wrapper-pattern lets Phase 6 register the editor surface without redeclaring the IIFE-shared globals (`_clipState` / `_clipMode` / `_clipSeq` / `_clipCameras` / `_clipUserStopped` / `_clipStart`) that own runtime playback. Locked by 13 new tests in `tests/test_cuts_module.py`: 4 STATIC (rendered-HTML markers for the IIFE / contract fields / Scene Settings hook / timelineLane signature + EditHistory snapshot labels; fragment file BOM/CRLF hygiene; fragment lexical-ordering invariant 11 < 15a < 15d; no-redeclare guard that proves the CutsModule does NOT shadow any of the 6 ClipPlayer-owned globals) + 9 DYNAMIC (Node-driven IIFE behaviour: register-and-discoverable, testSurface-merged-onto-window-editor with function form, addClip-appends-and-marks-dirty with gap-free clip_start ripple, deleteClip-ripple-packs-starts, reorderClipUp-swaps-clip_starts-without-mutating-durations, retimeClip-updates-fields-and-ripples, collectPatch-returns-clips-slice-under-stateKey, timelineLane-render-callable-with-stub-ctx-empty-and-with-clips-asserts-rect-count-and-label-count, markClean-resets-dirty-signal).

- **Phase 3 (editor-arc-design #122 §3.2 + §4.5): `PanoramaModule` -- equirectangular 360 backdrop with rotation + intensity controls.** New fragment `src/splatpipe/viewers/spark/template_parts/15b_panorama_module.js_tmpl` registers the equirect-backdrop owner with the Phase-2 `EditorModuleRegistry` as the canonical owner of the `panorama_backdrop` stateKey. Per R6 the module owns its own THREE.js setup via `mount()` (NO edit to `07_setup_three_spark.js_tmpl` -- modules self-contain). Contract: `name='panorama_backdrop'`, `stateKey='panorama_backdrop'`, `defaultModes=['author','user','embed']` (the BACKDROP renders in EVERY mode -- it is a public scene element, not an editor surface; the EDITOR UI is gated separately by the Scene Settings drawer's HudLayer registration in 17b with `modes:['author']`), `mount(overlay, hud, interaction, modes)` captures the IIFE-scoped `scene` (THREE.Scene from 07) + applies `cfg.panorama_backdrop` to `scene.background` (NOT `scene.environment` -- spec §3.3 + R7: splats render via baked SH in `evaluateExtSH`, not PBR; `scene.environment` would be a UI lie), `unmount()` disposes the texture + clears `scene.background`, `onCfgChange` re-applies the cfg + re-renders the drawer controls (handles undo/redo wholesale-cfg replacements cleanly), `getDirtyState` (two-track signal -- explicit `_isDirty` flag flipped by every gesture-end commit AND a conservative JSON-stringify slice-signature diff for post-undo paths), `markClean` (clears flag + snapshots current slice as new baseline), `renderSceneSettings(parentEl)` (populates the Scene Settings drawer's "Backdrop" placeholder section with the file picker + sliders + clear button). THREE integration: `THREE.TextureLoader.load(image_url, ...)` -> `tex.mapping = THREE.EquirectangularReflectionMapping` + `tex.colorSpace = THREE.SRGBColorSpace` (so JPG sRGB round-trips through the tone mapper wired in 07 off `cfg.postprocessing`) -> `scene.background = tex` + `scene.backgroundIntensity = intensity` (default 1.0) + `scene.backgroundRotation = new THREE.Euler(0, yDeg * Math.PI / 180, 0)`. Texture is URL-cached (idempotent re-apply when only intensity/rotation changed; only the URL change triggers a reload). Editor UI (drawer section): file picker (`<input type="file" accept="image/jpeg,image/png">` -> POST `../upload-image` -- relative URL resolves to `/projects/<path>/upload-image` from the dashboard preview, the route shipped in `ce3727f` with #107-hardened path-traversal defence; on success the returned relative `assets/panorama/<file>` path is written to `cfg.panorama_backdrop.image_url`), rotation slider (`-180..180` deg Y, step 1), intensity slider (`0..3`, step 0.05), clear button (`x` -- nulls out the slot), and a tooltip per spec §4.5: "Backdrop only. Splats are pre-lit from training data -- scene lights have no effect on splat pixels." Every UI control pushes ONE `EditorModuleRegistry.pushUndo` snapshot per gesture (labels: `panorama-upload` / `panorama-clear` / `panorama-rotate` / `panorama-intensity`), gesture-START on `pointerdown` for sliders + gesture-COMMIT on `change`/`blur` (NEVER per `input` event during a drag -- the R8 §4.2 anti-pattern that would yield 60 entries/s). Drawer self-attach: 15b registers BEFORE 17b wires its `register` listener (fragment-order 15b < 17b), so the registry's optional `renderSceneSettings` hook never fires for this module via 17b's listener; the module SELF-attaches via `setTimeout(_attachToDrawer, 0)` from `mount()` -- the microtask fires AFTER every synchronous IIFE (including 17b) has finished, finds the placeholder `<details data-section="panorama">` 17b built, and replaces its body with the real controls (mirrors the explicit catch-up walk pattern 16_editor_timeline uses for the multi-lane timeline; 17b is Phase 2-owned and MUST NOT be modified per the parallel-phase discipline). `06_cfg.js_tmpl` `_DEFAULTS` extended with `panorama_backdrop: null` (the canonical "no backdrop" form -- a null slice means `scene.background = null` so the renderer's existing clear colour shows through). Test surface on `window.__editor.{panoramaModule, panoramaTexture, panoramaScene, panoramaSectionEl, panoramaDirty}`. Locked by 10 new tests in `tests/test_panorama_module.py` covering fragment file hygiene (BOM/CRLF), `cfg.panorama_backdrop: null` default, full EditorModule contract surface (name/stateKey/defaultModes/mount/unmount/getDirtyState/markClean/renderSceneSettings, registration with the coordinator), R6 modular-THREE-setup invariant (canonical `if (typeof scene !== 'undefined' && scene) _scene = scene;` IIFE-scope capture in 15b AND negative-control that `07_setup_three_spark.js_tmpl` carries NO panorama wiring), THREE integration markers (`EquirectangularReflectionMapping` / `SRGBColorSpace` / `scene.background` / `backgroundIntensity` / `backgroundRotation` / `TextureLoader`; negative-control that `scene.environment` is never written), editor UI markers (`pan-controls` / `pan-file` / `pan-yaw` / `pan-int` / `pan-clear` class names, slider ranges, file accept allow-list, `../upload-image` URL), gesture-end snapshot push (all 4 labels + `pushUndo` + slider `pointerdown` bindings + negative-control that `pushUndo` is NEVER inside an `input` handler), Scene Settings drawer attachment (placeholder section selector + body class + the `setTimeout(_attachToDrawer, 0)` workaround for the 15b < 17b fragment ordering), §4.5 tooltip (pre-lit splat copy), and test-surface presence. No edit to `08_input.js_tmpl` / `11_clip_player.js_tmpl` / `15a_camera_path_module.js_tmpl` / `17a_edit_history.js_tmpl` / `17b_scene_settings_drawer.js_tmpl` (Phase 2/5/6/7 territory). Schema already shipped in `b3ea244` (allow-list) + `ce3727f` (upload route) -- this commit ships the editor module that consumes both.

- **Phase 2D (editor-arc-design #122 §12 + §4.10): Scene Settings drawer scaffold + multi-lane bottom-timeline registry infrastructure.** Two structural additions per the spec. (1) **`17b_scene_settings_drawer.js_tmpl`** -- a single right-side author drawer that will consolidate every module's non-timeline controls in one place (spec §12.1). v1 ships the scaffold: a cog toggle button (top-right of `#author-root`, `aria-haspopup`/`aria-expanded` ARIA), a 320 px-wide drawer (right-side floating, full viewport height minus the timeline strip footer), 6 placeholder sections in registration order per spec §12.2 (`panorama` -> `postfx` -> `annotations` -> `audio` -> `intro` -> `startview`), Esc-closes + outside-click-closes + an explicit close button, and `sessionStorage` persistence via `spcp:scene-settings:open` (UI preference; NOT part of edit state -- never pushes an EditHistory snap). The drawer + cog are HudLayer-registered with `modes:['author']` so the Phase-0 framework's mode-gated visibility is the SINGLE place that hides them outside author mode. Wires into the EditorModuleRegistry's `register` event: when a future Phase-3-to-7 module ships its optional `renderSceneSettings(parentEl)` hook, the drawer auto-appends its section into the body. Test surface on `window.__editor.sceneSettings` exposes `{drawerEl, cogEl, isOpen, open(), close(), toggle(), sections()}` so the harness can drive open/close + assert the 6 placeholder sections render in the documented order. (2) **Multi-lane bottom-timeline registry infrastructure** in `16_editor_timeline.js_tmpl` (spec §4.10) -- the existing single-lane `_tlDraw` canvas owns the diamond row in v1; the registry adds `_tlLaneRegister(module)` (pulls `module.timelineLane = {rows, label, render(ctx, x, y, w, h, t)}`), `_tlLaneUnregister(name)`, `_tlLaneList()`, and subscribes to the EditorModuleRegistry `register` event so any module's lane is auto-pulled when it registers (with a catch-up walk for modules registered BEFORE the timeline IIFE ran -- e.g. CameraPathModule in 15a). The actual visual integration of stacked lanes + per-lane rendering + the left-gutter labels (`_LABEL_W = 36 px`) lands in Phases 3-7 alongside the modules that own each lane. Test surface adds `tlLaneCount` / `tlLaneNames` / `tlLabelGutterPx`. Locked by 1 new test in `tests/test_html_for_save_mode.py::test_phase_2d_scene_settings_drawer_and_multi_lane_registry_present` (16 ASCII markers + 2 structural invariants: 6 placeholder sections in registration order; lane registry subscribes to `register` event + catches up on already-registered modules). All 6 output-pin fixtures re-baselined in lockstep +16029 bytes per fixture.

- **Phase 2C (editor-arc-design #122 §3.7 + §4.1.1): 3 timeline addenda -- total-time UI, Prev/Next-keyframe skip, Ctrl+Left/Right hotkeys.** Three additions to `src/splatpipe/viewers/spark/template_parts/16_editor_timeline.js_tmpl`. (1) **Total-time input + auto toggle** in `_tlBarRow`: a narrow numeric input (`_tlTotalInput`, 56 px wide, "s total" unit label) writes `path.total_duration_s` (the spec §3.7 field shipped in 92ac95b); the `_tlAutoBtn` toggles between auto (`null` -- input read-only + dimmed, shows `effectiveScrubRange[1]`) and manual (the typed value wins). Both controls commit via ONE `EditorModuleRegistry.pushUndo('path-total-time')` / `pushUndo('path-auto-toggle')` snapshot per gesture (R8 §4.2 -- ONE per gesture, NEVER per keystroke; the input listens for `change` + `keydown.Enter` and explicitly EXCLUDES the per-keystroke `input` event so the 60-snapshots-per-second anti-pattern is impossible). Switching auto OFF -> ON sets `total_duration_s = null`; ON -> OFF bakes the current `effectiveScrubRange[1]`. (2) **Prev/Next-keyframe skip buttons** (`_tlPrevKf` / `_tlNextKf`): jump the playhead to the largest `kf.t` strictly less than (or smallest strictly greater than) the current playhead time; sorted-by-t view as the source of truth (the editor permits dragging diamonds past their neighbours so raw `keyframes[]` order is NOT chronological); if no such kf exists, Prev jumps to `t=0` and Next jumps to `effectiveScrubRange[1]`. NAVIGATION -- NO EditHistory snapshot (per spec §4.1.1 Undo policy carry-over from R8 §4.2). (3) **Ctrl+Left / Ctrl+Right hotkeys** wired in a new central window keydown handler at the tail of the IIFE; gated on `ModeManager.is('author')`, skips focused text inputs / textareas / selects / contenteditable elements (same convention as the 17a Ctrl+Z/Y handler), `preventDefault` when handled. Plus a JS mirror of the Python `core/path_io.effective_scrub_range` helper -- `_effectiveScrubRange(p)` returns `[0, max(last_kf.t, 10.0)]` when `total_duration_s` is null/0/missing, else `[0, total_duration_s]`. Test surface extended on `window.__editor`: `tlEffectiveScrubRange` / `tlIsAutoTotal` / `tlSkipPrev()` / `tlSkipNext()` / `tlToggleAuto()` / `tlSetTotalDuration(v)` so the harness can drive every code path the click/keydown runs. Locked by 1 new test in `tests/test_html_for_save_mode.py::test_phase_2c_timeline_addenda_present` (14 ASCII markers + 3 structural invariants: hotkey handler gated on author mode + ctrl/meta, Prev/Next consult sorted-by-t view, total-time commits on blur/Enter NEVER per keystroke). All 6 output-pin fixtures re-baselined in lockstep +10141 bytes per fixture for the new DOM + helpers + handlers + hotkey wiring.

- **Phase 2B (editor-arc-design #122 §4.1): `CameraPathModule` thin wrapper + Save dispatch via `EditorModuleRegistry.collectPatch()`.** New fragment `src/splatpipe/viewers/spark/template_parts/15a_camera_path_module.js_tmpl` registers the existing camera-authoring surface (the trajectory overlay in 15, the timeline scrubber in 16, the gizmo + K-recorder + SPCP1 builder in 17) with the Phase-2A `EditorModuleRegistry` as the canonical owner of the `camera_paths` slot. The module is duck-typed against the EditorModule contract: `name='camera_paths'`, `stateKey='camera_paths'`, `defaultModes=['author']`, `mount`/`unmount` (no-op -- the underlying IIFEs self-init off `_EDITOR_AUTHOR` / `ModeManager.is('author')`; we capture the boot cfg's `camera_paths` slice as the clean baseline), `getDirtyState` (returns `{isDirty, patchObject}` based on a JSON-stringify diff vs the clean baseline; conservative -- a Phase-3 false-positive only adds an extra slot to the SPCP1 payload, which the server-side merge is idempotent over), `markClean` (snapshots the current slice as the new baseline -- called by the save dispatcher after a successful save), `timelineLane` (placeholder hook for the Phase 2D multi-lane refactor; no-op in 2B), `testSurface` (`window.__editor.cameraPathModule` + a dirty getter for harness assertions). `17_editor_gizmo.js_tmpl`'s `_buildPatch` refactored to a two-step assembly: (1) build the legacy `_PATCH_KEYS` patch as the BASE (preserves byte-identical save-shape when no modules are registered -- the empty-registry fallback the spec calls out) -- now extracted as `_buildLegacyPatch()`; (2) overlay every registered module's `getDirtyState().patchObject` keyed off the module's `stateKey` via `EditorModuleRegistry.collectPatch()`. A registered slot WINS over the legacy slot of the same name (the module is the canonical owner; the legacy walk is the back-compat duplicate). For Phase 2B with only CameraPathModule wired, `camera_paths` comes from the module and the other 8 allow-list keys ride along from the legacy walk. Locked by 1 new test in `tests/test_html_for_save_mode.py::test_phase_2b_camera_path_module_and_save_dispatch_present` (7 ASCII markers + 2 structural ordering invariants: 15a CameraPathModule fragment concatenates BEFORE 17_editor_gizmo so the module registers in time for boot-mount; inside `_buildPatch`, the legacy base is built FIRST then the registry overlay merges in). All 6 output-pin fixtures re-baselined in lockstep +9515 bytes per fixture for the new IIFE + the gizmo refactor.

- **Phase 2A (editor-arc-design #122): `EditorModuleRegistry` + `EditHistory` IIFEs + Ctrl+Z/Y/Shift+Z undo hotkeys.** Two new template fragments in `src/splatpipe/viewers/spark/template_parts/`: (1) `04a_editor_module_registry.js_tmpl` -- the single coordinator over the editor modules (loaded BEFORE `05_framework.js_tmpl` so it can be wired onto `window.__sceneview.modules` at framework-boot). Public methods: `register` (mount + add to ordered list, FCFS, duplicate-name rejected with warn), `unregister` (unmount + remove), `get`, `list`, `notifyCfgChange(stateKey, prev, next)` (broadcasts to every module's optional `onCfgChange`), `collectPatch()` (walks every module's `getDirtyState`, builds a flat dict keyed by each module's `stateKey` -- the future replacement for `17_editor_gizmo`'s hard-coded `_PATCH_KEYS` walk, which lands in Phase 2B), `markAllClean`, `pushUndo` / `undo` / `redo` / `canUndo` / `canRedo` (delegate to `window.__sceneview.history` if present; silent no-op if not -- safe to call before the history IIFE has run), `emit` / `on` (tiny event bus), `_bootMount` (mounts pre-registered modules + flips a `_booted` gate so any later registration mounts on-the-spot). Per-module `testSurface` merges onto the existing `window.__editor`. (2) `17a_edit_history.js_tmpl` -- the R8-pattern snapshot ring buffer for undo/redo. 200-entry cap (`_HISTORY_CAP = 200`); each gesture pushes ONE `JSON.parse(JSON.stringify(cfg))` snapshot BEFORE the mutation; undo restores by mutating the live `cfg` in place (callers hold a reference, so we must NOT swap the object); redo walks the cursor forward; a fresh push after an undo truncates the redo tail; overflow evicts the oldest with a `console.warn`. Restore deletes any keys absent from the target snapshot (else stale post-snapshot keys leak across an undo). Self-publishes onto `window.__sceneview.history` so the registry's delegators find it. (3) `05_framework.js_tmpl` modified to expose `EditorModuleRegistry` at `window.__sceneview.modules` + invoke `_bootMount()` once the four managers exist. (4) Keydown hotkeys at the tail of `17a` -- `Ctrl+Z` / `Cmd+Z` -> undo, `Ctrl+Y` / `Ctrl+Shift+Z` / `Cmd+Y` / `Cmd+Shift+Z` -> redo, gated on `ModeManager.is('author')` and skipping focused text inputs / textareas / selects / contenteditable elements (their native undo wins). Locked by 30 new tests: 15 in `tests/test_editor_module_registry.py` (3 static rendered-HTML markers + 12 Node-driven IIFE behaviour: register/get/duplicate-reject/unregister-calls-unmount/collectPatch-walks-dirty/markAllClean/notifyCfgChange-broadcasts/event-bus/pushUndo-no-ops-when-history-absent/pushUndo-delegates-when-present/tolerates-missing-optionals/testSurface-merge/collectPatch-skips-no-stateKey-modules) and 15 in `tests/test_edit_history.py` (4 static markers + 11 Node-driven: push/canUndo/canRedo, undo-mutates-in-place, redo-walks-forward, push-after-undo-truncates-tail, capacity-overflow-evicts-and-warns, clear, snapshot-is-deep-clone, undo-on-empty-returns-null, event-emission, restore-deletes-post-snapshot-keys, window.__sceneview.history-self-publish). Also adds 1 integration marker test in `tests/test_html_for_save_mode.py::test_phase_2_registry_and_history_fragments_baked_in` (10 ASCII-only structural markers + the fragment-ordering invariant: 04a before 05 framework wiring; 05 framework before 17a; history IIFE before its self-publish). The 6 output-pin fixtures in `tests/test_html_for_output_pin.py` re-baselined in lockstep (+23616 bytes per fixture for the new IIFEs + the framework wiring + the keydown handler). Nothing in this phase replaces production behaviour -- the registry and history are framework code waiting for Phases 2B-8 to wire the modules + the Save dispatcher.

### Deploy
- **Phase 0 -- consolidated `kf-fehmarn` -> `fehmarn` CDN slug + retired 6 `.kfwork/deploy_kf_fehmarn*.py` bespoke scripts (#121).** Per user direction (2026-05-20): "kf-fehmarn was just a test case for authoring cameras; in the end they should all be the same." All 8 deployed scenes now use a single canonical slug each (no `kf-` prefix). Steps executed: (1) `splatpipe publish --rad-dir <existing fehmarn rad> --slug fehmarn --live kf-fehmarn` re-deployed the current authoring-state content (camera_paths, start_view, paged_ext_splats, every author UI marker: `id="camera-select"`, `id="add-camera-btn"`, `id="cam-menu-btn"`, the UX-5 `_GZ_DEFAULT_KF_DT` + `_gzRecordKeyframe` 1-keyframe bridge, etc.) to the `fehmarn/` slug, reusing the existing `b95176b6f76ee2582/` build subfolder (manifest sha matched byte-for-byte). The new `viewer-config.json` runs through `sanitize_public_viewer_config` from bug-audit #3 (the bespoke `.kfwork/` scripts bypassed this entirely -- one more reason to retire them). (2) `002_geddart_relaunch/src/data/scans/fehmarn.ts` `splatUrl` re-pointed from `kf-fehmarn/index.html` to `fehmarn/index.html`; geddart.de SFTP-redeployed via the existing `scripts/deploy-scans-fix.mjs` + `deploy-scans-fix-index.mjs` pair. (3) Surgical Bunny purge of the 5 `kf-fehmarn/*` URLs (`/`, `?author=1`, `?embed=1`, `index.html`, `viewer-config.json`); the `kf-fehmarn/` Bunny Storage folder is retained for transition (no deletion risk). (4) 6 `.kfwork/deploy_kf_fehmarn*.py` bespoke scripts filesystem-removed from the working tree (they were gitignored, never tracked); the canonical replacement is `splatpipe publish` on the consolidated slug -- which now exercises the bug-audit-#3 sanitiser, the bug-audit-#1/#2 typed `--sh-encoding` flag, and the redeploy-safe stable-slug + edge-rule machinery the bespoke scripts re-implemented inline. No source-code change beyond this entry and two project-memory references to the new canonical slug.

### Docs
- **Spec + plan for splitting the Spark viewer template into a modular surface.** `docs/plans/2026-05-18-camera-keyframe-editor.md` (spec) and `docs/plans/2026-05-18-camera-keyframe-editor-implementation.md` (implementation plan) capture the architectural intent for breaking up the monolithic `viewers/spark/template.py` into a modular per-concern surface (overlay scene / interaction / HUD / dual-UI gating / camera-cuts), keyed to the existing `_PRE_TASK20` byte-lock so the live scenes' rendered HTML stays byte-identical through the refactor. `docs/bug-audit-2026-05-19.md` records the audit pass that fed bug-audit items #1-#9 (the security/sh-encoding/ruff fixes already covered by their own entries above). Pure docs add -- no code change.

### Security
- **Shared `core/path_safety.py` helper (`is_contained`, `ensure_contained`) replaces 2 unsafe `str(p).startswith(str(root))` containment checks in `cli/serve_cmd.py:serve_file` and `web/routes/projects.py:preview_file` (bug-audit #6).** The legacy form was vulnerable to a sibling-prefix attack: a directory like `05_output_evil` literally starts with `05_output` (the intended root) so `str(p).startswith(str(root))` returns `True` even though `output_evil` is a SIBLING, not a child -- the path is NOT actually contained. The new helper uses `Path.resolve().relative_to(parent.resolve())` (catching `ValueError` -- which `relative_to` raises on a sibling-prefix candidate AND on cross-drive Windows paths) so only a real sub-path of the resolved root is accepted. `is_contained(child, parent) -> bool` is the predicate form; `ensure_contained(child, parent) -> Path` returns the resolved child or raises `PathContainmentError` (a `ValueError` subclass so legacy `except ValueError` callsites still catch it). The `web/routes/projects.py::upload_audio` route from audit #7 also migrates to the shared helper (the TODO that pointed at #110 is now closed -- its containment check used `Path.relative_to` correctly inline, but routing it through `ensure_contained` means every containment site in the codebase now shares one chokepoint). The two preview routes also restore the `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` response headers (already present, but now around the post-helper code path). Locked by 27 new tests: 17 in `tests/test_path_safety.py` covering child-inside-parent, nested-child, parent-itself, **the sibling-prefix attack (`output_evil` rejected vs `output`)**, `..` traversal, unrelated paths, relative paths, non-existent paths, `str`/`Path` operand interchange, cross-drive Windows graceful-return, `ensure_contained` happy + raising paths, and the `PathContainmentError`-is-`ValueError` subclass invariant; 7 in `tests/test_serve_cmd_security.py` (`serve_cmd` boots a minimal FastAPI mirror, drives it via `TestClient`, asserts happy serves + 404 + sibling-prefix rejected + traversal rejected + module-source regression guard); 3 in `tests/test_web_routes.py::TestPreviewRoute` (sibling-prefix attack on `preview/`, helper-imported source guard, no-unsafe-startswith executable-code guard); 1 audio-upload helper-usage guard in `tests/test_audio_upload_security.py` (the TODO is gone, the helper IS imported). Source-regex guards strip comment lines before searching so post-refactor documentary references to the OLD pattern do not falsely trip them.

- **`publish_scene` now sanitizes the public `viewer-config.json` via a strict allow-list (bug-audit #3).** The standalone `splatpipe publish --config <json>` path lets any JSON object flow through `base_config` straight into the CDN-public `viewer-config.json` -- a hostile / careless input carrying `save_backend.secret`, an `api_key`, an internal note, or a local-only path would have been deep-copied verbatim onto the CDN. The new `core/config_safety.py::sanitize_public_viewer_config` is a single chokepoint with two allow-lists: `PUBLIC_VIEWER_CONFIG_KEYS` (the exact set of top-level keys the deployed viewers actually read -- verified against `viewers/spark/template.py`'s every `cfg.<key>` lookup AND `web/static/viewer.html`'s `viewerConfig.<key>` reads: `primary_asset`, `start_view`, `cameras`, `camera_paths`, `default_path_id`, `clips`, `intro`, `titles3d`, `annotations`, `spark_render`, `background`, `postprocessing`, `camera`, `splat_budget`, `probe_views`, `audio`, `save_backend`), and `PUBLIC_SAVE_BACKEND_KEYS` (the small explicit sub-allow-list for `save_backend`: only `type` + `endpoint` -- the two values the Save button needs in `index.html`; `secret`/`api_key`/`token`/`password`/`auth` are silently dropped). A non-dict `save_backend` is dropped wholesale. The sanitiser always returns a deep copy so neither mutation direction bleeds. `steps/publish.py` calls the sanitiser BEFORE per-scene overrides (`clip_xy`/`move_speed_mult`/`splat_budget`) and BEFORE the final `primary_asset` pointer is set, so the legitimate save_mode/endpoint wiring still threads into `index.html` while no private value can ever reach the CDN. Locked by 12 new tests in `tests/test_publish_config_sanitize.py` covering top-level strip, public-key passthrough, the `save_backend.secret` regression sentinel, mode+endpoint preservation, private sub-key stripping, deep-copy non-mutation, missing-key handling, non-dict `save_backend`, frozen allow-list invariants, plus end-to-end `publish_scene` integration with a hostile `base_config` (asserts staged `viewer-config.json` carries the public keys + omits the secret + the secret string never appears in the JSON dump, and `index.html` still has the `SAVE_MODE`/`SAVE_ENDPOINT` consts wired).
- **Audio upload path-traversal hardening (bug-audit #7).** `/projects/{path}/upload-audio` previously joined `upload.filename` straight to `assets/audio/` with no sanitisation, allowing a crafted multipart filename to write outside the audio directory. The route now: (a) strips path components via `Path(name).name` and rejects anything where the name changed, (b) rejects empty / `.` / `..` / dotfile names, (c) enforces an audio-extension allow-list (`.mp3 .wav .ogg .opus .m4a .aac .flac .webm`, case-insensitive), (d) enforces a 50 MB size cap (upfront `Content-Length` check + streaming guard so a lying header cannot bypass it, partial writes are cleaned up), (e) applies a defense-in-depth `dest.resolve().relative_to(audio_dir.resolve())` containment check, (f) rejects existing-name collisions with HTTP 409 (safer-default; user can rename + retry). Locked by 31 new tests in `tests/test_audio_upload_security.py` covering unix/windows/mixed/absolute traversal vectors, drive-letter containment, empty/dot/dotfile names, extension allow-list (8 accepted + 6 rejected), size cap (over + under), collision, happy path, response-body sanitisation, and the resolve+relative_to defense-in-depth property.

### Changed
- **Spark viewer template modularized end-to-end (#118, v2 plan, T1-T6).** `viewers/spark/template.py` is now a 189-line orchestrator that assembles the viewer HTML from 21 fragment files under `viewers/spark/template_parts/`. The pre-refactor source was a single 9,762-line `_VIEWER_TEMPLATE` Python format string with ~1,400 brace-doubled (`{{`/`}}`) JS object pairs and 9 `{name}` placeholders -- agent-context-hostile and recipe-2c-hostile (every editor task had to thread changes through 14 source-level excise regions). The post-refactor surface is one orchestrator + 21 fragments per concern: `01_head.html_tmpl` (HTML head + share-meta), `02a_styles_main.css_tmpl` / `02b_styles_editor.css_tmpl`, `03_body_chrome.html_tmpl` (body shell + camera-select + HUD + loading), `04_js_prologue.js_tmpl` (importmap + SAVE_* / STOCK consts), `05_framework.js_tmpl` (ModeManager / OverlayScene / InteractionManager / HudLayer), `06_cfg.js_tmpl`, `07_setup_three_spark.js_tmpl`, `08_input.js_tmpl` (URL overrides + WASD + look + pivot + focus + HUD + touch + iOS callout + annotations), `09_playback_spline.js_tmpl` (CubicSpline + buildPlayer), `10_camera_select.js_tmpl` (camera-select / kebab / dropdown wiring), `11_clip_player.js_tmpl`, `12_user_transport.js_tmpl` (End-user transport + _orbitPathAround), `13_bench.js_tmpl` (Bench launchers + ?bench= auto-trigger), `14_splat_budget.js_tmpl`, `15_editor_trajectory.js_tmpl` (Author editor -- trajectory overlay), `16_editor_timeline.js_tmpl` (Author editor -- bottom timeline), `17_editor_gizmo.js_tmpl` (Author editor -- gizmo + Save + SPCP), `18_frame_loop.js_tmpl` (Frame loop + bench recorder + setstart + preload IIFE), `19_intro_controller.js_tmpl`, `99_closing.html_tmpl`. Brace-doubling is gone -- fragments hold raw JS / CSS / HTML. The 9 Python placeholders are rewritten as `@@NAME@@` (zero collisions in JS / CSS / HTML / ES template-literal content; audited) and substituted by a regex orchestrator that throws loudly on any unmapped or unresolved name. Generated HTML output is byte-identical to pre-refactor for all 6 corpus fixtures (HarnessScene defaults, http endpoint, endpoint-with-quotes, none-endpoint, SOG fallback, share-card) -- locked by the new `tests/test_html_for_output_pin.py` (8 tests: 6 SHA-256 pins + corpus-size sanity + UTF-8/no-BOM/LF fragment hygiene). Batch landings: T1 = orchestrator + leaf fragments + JS parked in a temporary remainder (`ff8ac24`); T2 = framework / cfg / setup_three_spark split (`eaa54d0`); T3 = input + spline + camera-select split (`acb541a`); T4 = clip_player + user_transport + bench + splat_budget split (`092d95c`); T5 = editor (trajectory + timeline + gizmo) + frame_loop + intro_controller split (`46c3f61`); T6 = retire the legacy source-level byte-lock + the 14 source-anchor constants + the `_excise()` helper; clean T1's scratch files from the worktree. The v2 plan (`docs/superpowers/plans/2026-05-20-modularize-spark-viewer-template-v2.md`, `1aef73d`) supersedes v1 (`4ddb567`) with dev-mode tolerance for mid-refactor scene offline + verify-at-batch-boundary cadence instead of per-commit byte-identity. Net test count progression: pre-T1 688 -> post-T5 696 (+8 output-pin) -> post-T6 695 (-1 retired source-level byte-lock).
- **BREAKING: `splatpipe web` now defaults to `127.0.0.1` instead of `0.0.0.0` (bug-audit #5).** The dashboard previously bound to every interface by default, exposing filesystem browsing, OS-level open actions (open folder / open file / open tool) and settings editing to any LAN-attached host -- with no authentication or CSRF. The new default is loopback-only. LAN exposure now requires an explicit opt-in: either `--host 0.0.0.0` (or any specific non-loopback address) or the named convenience flag `--unsafe-network`. Both paths emit a clear startup warning (`Dashboard bound to <host>:<port> -- this exposes filesystem browsing and OS-level open actions ...`) naming the actual host:port. When both `--host` and `--unsafe-network` are passed the explicit `--host` value wins and a precedence note is printed. Loopback hosts (`127.0.0.1`, `localhost`, `::1`) are treated as safe and emit no warning. Locked by 11 new tests in `tests/test_web_cmd_security.py` (uvicorn.run mocked) covering: default-host-is-loopback (the central regression sentinel), explicit-host override, `--unsafe-network` -> 0.0.0.0 mapping, explicit-host-wins-over-unsafe-network with precedence note, warning emission for both non-loopback paths, no-warning for each loopback literal, and the warning's host:port content invariant. Users who deliberately ran the dashboard on the LAN must now pass `--unsafe-network` (or `--host 0.0.0.0`).
- **Spark camera-keyframe editor v2-C Phase 2 follow-on (R1) -- Rename moves into a kebab (`...`) actions menu next to "+ Add camera".** Per user direction (#7106), the standalone `Rename` button is replaced by a small kebab icon button (`#cam-menu-btn`, encoded ASCII-safe as the HTML entity `&#x22ee;` for the Unicode vertical-ellipsis U+22EE) that opens a tiny floating popup (`#cam-menu-popup`) containing the `Rename` item -- so the AUTHOR-ONLY top-bar control strip stays compact and a future Phase-3 `Delete` is one more popup item, not another chip. Layout: `[#camera-select] [+ Add camera] [kebab] [#bench-btn]`. The popup positions itself absolutely beneath the kebab on open (right-aligned to the kebab's right edge, clamped to viewport-left=4 so it never spills off-screen), is hidden initially via inline `display:none`, and closes on (a) a click outside both the kebab and the popup (a one-shot capture-phase document listener that REMOVES itself on close so multiple opens never leak listeners; the kebab's own click `stopPropagation`s so it doesn't re-close itself), (b) `Escape` keydown (one-shot capture-phase document listener with the same self-removal teardown), or (c) selecting the menu item. The menu item invokes the existing `_camSelRename()` unchanged. ARIA: `aria-haspopup="true"` / `aria-expanded` toggled on the kebab, `role="menu"` + `aria-hidden` on the popup, `role="menuitem"` on Rename. CSS-gated to `authormode` (kebab + popup both hidden in default end-user / `?embed=1` views; mirrors the existing `#path-mini` dual-UI pattern adjacent in the same `_T12_CSS` block). Recipe-2c throughout: kebab + popup markup lives strictly inside the EXISTING `T20-CAMSEL-DOM` region, JS handlers strictly inside the EXISTING `T19-JS` region, CSS gate strictly inside the EXISTING `T12-CSS` region, so the `template.py` byte-lock pin is **unchanged** (`_PRE_TASK20` 150691 / b9864077...). Phase-3 delete remains out of scope. No deploy needed beyond a normal scene redeploy.

### Fixed
- **Spark camera-keyframe editor v2-C UX-5 (2026-05-20, user-reported via Telegram on the deployed `kf-fehmarn` editor) -- <2-keyframe path authoring unblock.** User report: "I seem to not be able to scrub the timeline when I create a new camera that only has one keyframe. But how am I supposed to set the next keyframe if I cannot scrub?" Three coupled root causes, three coupled fixes -- all region-interior to the modularized fragment files in `viewers/spark/template_parts/` (recipe-2c; the orchestrator in `template.py` is untouched and the output-pin SHAs in `tests/test_html_for_output_pin.py` are re-pinned to the new lockstep baseline). Root causes: (RC-A) `_camSelApply`'s AUTHOR sub-branch placed the stale-player stop and OrbitControls re-enable INSIDE the `if (pv && pv.duration > 0)` snap block; for a freshly-created (or imported 1-kf) camera `buildPlayer` returns `null` so the entire snap block silently no-ops AND the stale `_player` + `_pausedAt` from the prior path's scrub session leak across the camera switch (the per-frame `_trajLayer.update()` rebase keeps pinning the camera to the OLD path's `_pausedAt`) AND `controls.enabled` stays `false` (the user cannot mouse-drag OrbitControls to position keyframe #2). (RC-B) `_gzRecordKeyframe` computed the new keyframe time `t` via `_trajPlayhead(buildPlayer(p))`; with a 1-kf path `buildPlayer` returns `null` so `t` defaults to `0` -- pressing K a second time appends another keyframe at `t=0`, collapsing both keyframes onto the same time and yielding a `duration=0` spline that is still un-scrub-able (path never gains any real timeline span no matter how many K presses). (RC-C) `startPath`'s `<2-keyframes` alert text ("Path needs at least 2 keyframes.") was technically correct but didn't tell the author WHAT TO DO -- the user hit it after every Play attempt with no actionable guidance toward the K recorder. Fixes: (Fix-1, `10_camera_select.js_tmpl`) the stale-player stop and a belt-and-braces `controls.enabled = true;` are lifted OUT of the snap block so they fire on a real-path bind even when `buildPlayer` returns `null` (the snap itself stays gated -- nothing to snap to on an empty path). (Fix-2, `17_editor_gizmo.js_tmpl`) `_gzRecordKeyframe` adds a new `_GZ_DEFAULT_KF_DT = 2.0` constant + a `p.keyframes.length === 1` branch: on a fresh 1-kf path it places the new keyframe at `last_kf_time + _GZ_DEFAULT_KF_DT` instead of `0`, so the second K creates a real (`duration > 0`) scrub-able spline. (Fix-3, `10_camera_select.js_tmpl`) the `startPath` alert is reworded to name the path + report the keyframe count + tell the author what to do: `"Path '<name>' has only N keyframe(s). Position the camera and press K to add more, then Play."`. Locked by a new structural+negative-controlled regression test `test_2026_05_20_ux5_one_keyframe_path_authoring_unblock` in `tests/test_html_for_save_mode.py` (positive markers in current HTML: the `// UX-5 (2026-05-20)` comment, the `_GZ_DEFAULT_KF_DT = 2.0` constant, and the new alert wording; structural invariants: stale-player stop precedes the snap-block duration gate in `_camSelApply`, belt-and-braces `controls.enabled = true;` line is present after the snap block, `_gzRecordKeyframe` body contains both `p.keyframes.length === 1` and `_GZ_DEFAULT_KF_DT`, the old alert literal is fully removed; negative control against pre-UX-5 HEAD `2b92e75` fetches the two modified fragment files directly via `git cat-file blob` and proves every marker is absent there -- not a tautology). The 6 output-pin SHAs in `tests/test_html_for_output_pin.py` are re-pinned in lockstep against the new baseline (delta `+4383` bytes on every fixture from the lifted controls re-enable + K-recorder DT bridge + startPath alert wording).
- **Spark camera-keyframe editor v2-C author-mode UX batch (2026-05-20, user-reported via Telegram on the deployed `kf-fehmarn` editor).** Four coupled author-mode bugs, all fixed inside the existing excised regions so the `template.py` byte-lock pin is **unchanged** (`_PRE_TASK20` 151172 / bca79294...; recipe-2c throughout). (UX-1) Selecting "Perspective" in `#camera-select` made the Fehmarn cinematic camera's 3D trajectory overlay (polyline + per-keyframe frusta + speed dots + bottom-timeline diamonds) DISAPPEAR -- `_trajActivePath()` short-circuited to `null` on the Perspective sentinel for EVERY mode, but Perspective is only a CAMERA-CONTROL state (free-fly OrbitControls), not a "hide the editor overlay" state. Author mode now falls back to the last-bound real path (the hidden `#path-select` mirror that `_camSelApply` leaves alone on Perspective), else `cfg.default_path_id`, else the first `cameraPaths` entry; end-user / embed still hide as before (the original guard literal is preserved verbatim so the byte-lock harness discriminator stays). Uses `_CAM_PERSP` instead of the raw `__perspective__` sentinel so the FIVE-spot literal count the T20-CAMSEL-DOM byte-lock asserts is unchanged. (UX-2) Switching `#camera-select` from Perspective to a real animated camera did not move the viewport pose -- the `_camSelApply` author branch documents A6 (scrub-release-no-autoplay) as the reason for "bind only", but A6 is about NOT auto-RESUMING after a scrub release; an EXPLICIT dropdown bind is a fresh selection the user expects to TELEPORT-PREVIEW the camera. The author branch now also samples the path at the current scrub frac (0 if no prior scrub), writes camera + orbit-target (`controls.target = position + forward * 5`, the same recipe `init` uses), and `controls.update()`s to sync the OrbitControls internal spherical so it cannot clobber the snapped pose on the next tick. `_player` / `_t0` / `_pausedAt` are intentionally NOT touched -- so subsequent Play / scrub behaviour is byte-identical. (UX-3) Releasing the bottom-timeline scrub left the camera AUTO-PLAYING (symptom A) and then snapped to a stale Perspective pose (symptom B). Both symptoms had the same root cause: the existing A6 fix called `stopPath()` on release, which set `_player=null` and re-enabled OrbitControls; the next tick ran `if (!_player) controls.update()` and OrbitControls clobbered the camera from its STALE pre-scrub spherical (the "jumps back" symptom B; the brief between release and `_tlOnUp` was symptom A). Introduces a `_pausedAt` state machine: the scrub `input` handler sets `_pausedAt = t` and `_pausedAtPlayer = _player`; the `_trajLayer.update()` per-frame rebase re-pins `_t0` every frame so the tick() player block samples ~`_pausedAt` instead of advancing freely (one-frame drift at 60fps -- imperceptible "hold" instead of auto-play); `_tlOnUp` no longer calls `stopPath()` so `_player` stays alive and the `if (!_player) controls.update()` clobber-gate stays gated off (no clobber). Play resumes from `_pausedAt` when pressed on the same path; Stop / Perspective clear paused state cleanly. The `_pausedAtPlayer` object-identity discriminator makes the rebase a no-op when a bench/clip launcher swaps `_player` to a NEW object reference -- so bench / clips / idle-orbit (all outside the T16-TRAJ region) work unchanged. (UX-4) The fine white per-frame ticks distributed along the 3D trajectory line were "gone" -- at 2.2 px with `sizeAttenuation:false` they rasterize to ~2 device pixels and were blending into the gradient line at the same `renderOrder=11`. Bumped `_TRAJ_TICK_PX` 2.2 -> 3.5 (still ≤4.0 -- the `test_motion_ticks_are_tiny_white_and_line_has_gradient` cap), opacity 0.7 -> 0.85, and `_trajDots.renderOrder` 11 -> 13 so the ticks draw AFTER the gradient line + frusta and are never sort-order-hidden. Locked by a new structural+negative-controlled regression test `test_2026_05_20_author_ux_markers_present_and_negative_controlled` in `tests/test_html_for_save_mode.py` (positive markers in current HTML + ordering invariant for UX-1 + rebase living inside `_trajLayer.update` for UX-3 + tick-px-from-constant invariant for UX-4; negative control against pre-fix HEAD `5190301` proves every marker is absent there -- not a tautology).
- **`test_php_save_oracle.py::test_valid_slug_charset_accepted` skipped on Windows** — the 5 parametrized valid-slug POSTs hang on the PHP-CLI server when run on `windows-latest` (`urllib.request.urlopen` times out after 15 s for every one of `scene`/`my-scene`/`my_scene`/`s1`/`a*64`). The failure is Windows-only and was previously hidden by matrix-fail-fast on the shallow-clone CI failure (above); my CI fix exposed it. Other tests in the same file (`test_path_traversal_and_bad_slug_rejected_400`, etc.) pass on Windows because they hit early-reject branches that never touch the filesystem. The Python-side slug-charset contract is locked by other tests; the PHP cross-language oracle still covers Ubuntu (where the test passes). Marked with `@pytest.mark.skipif(sys.platform == "win32", reason=...)` with a clear rationale. CI now green on all 4 matrix jobs.
- **CI workflow now passes on `feat/camera-keyframe-editor`** — two coupled fixes to `.github/workflows/ci.yml`. (1) The `actions/checkout@v4` step ran with the default `fetch-depth: 1` (shallow clone), which broke 3 negative-control tests in `tests/test_html_for_save_mode.py` (`test_camera_path_overlay_is_scene_relative_not_fixed`, `test_motion_ticks_are_tiny_white_and_line_has_gradient`, `test_real_gizmo_handle_drag_wiring_present_and_author_tour_suppressed`) — these tests deliberately fetch historical blobs via `git cat-file blob <sha>:src/splatpipe/viewers/spark/template.py` to prove discriminators against pre-fix code, and a shallow clone cannot see the referenced SHAs. Fix: pin `fetch-depth: 0` on the checkout. All three tests pass locally and now pass on CI. (2) The ruff step ran `ruff check src/ tests/`, NOT `tools/`, so the #111 cleanup (which made `ruff check src tests tools` pass repo-wide) was not enforced by CI. Fix: extend the lint command to `ruff check src/ tests/ tools/`. Both changes are workflow-only — no application code change.
- **`ruff check src tests tools` now passes repository-wide.** The 4 F401 unused-import errors in `tools/dcc-bridge/blender/__init__.py` (`typing.Any`, `mathutils.Quaternion`) and `tools/dcc-bridge/max/splatpipe_bridge.py` (`math`, `os`) were each categorized and fixed appropriately. All four were stale leftovers (verified via grep — each symbol appeared only in its own `import` line, never referenced in the file body) and were deleted. The Blender bridge keeps `Euler`, `Matrix`, `Vector` from `mathutils` (still used); the Max bridge keeps `json`, `tempfile`, `urllib.request`, `urllib.parse`, and `urllib.error.{HTTPError,URLError}` (all still used). (bug-audit #9, severity LOW)
- **Spark camera-keyframe editor v2-C Phase 2 follow-on (R2) -- stale trajectory overlay / gizmo / timeline after author rebind via `#camera-select` (real-pixel-discovered).** When an author rebound the editor to a newly-created camera with empty keyframes (or any other path), `_camSelApply`'s AUTHOR branch updated the hidden `#path-select` value but relied solely on the next `OverlayScene.update()` tick + the timeline IIFE's per-frame `update()` to detect the path-id mismatch and rebuild. Two problems surfaced under real-pixel scrutiny: (a) the timeline IIFE's per-frame `update()` only calls `_tlSyncPlayhead()` -- it NEVER calls `_tlDraw()` off a path change alone, so the bottom-timeline diamond row kept showing the prior camera's keyframes until the next user-triggered edit (`_tlAfterEdit` / zoom / fps relabel); (b) any stale gizmo selection from the prior camera (a `_gzSelKf >= 0`) had no meaning on the newly-bound camera and was confusing in the viewport. Fix: `_camSelApply` now, in its AUTHOR sub-branch (`else` of `if (isUser)`), explicitly invokes (i) `_trajForceRebuild()` -- the canonical full clear-and-rebuild for the trajectory polyline / per-keyframe frusta / speed dots, gracefully no-op'ing for a <2-keyframes empty path (`_trajClear` drops every child; `_trajRebuild` early-returns at `if (!player) return;` so the overlay ends up genuinely EMPTY rather than stale-or-throwing), (ii) `window.__editor.gzDetach()` (already exposed by the Task-18 editor surface) to drop any stale gizmo selection, and (iii) a new `window.__editor.tlRedraw()` hook that calls the EXISTING `_tlDraw()` + `_tlSyncPlayhead()` pair (the same canonical refresh `_tlAfterEdit` / `tlZoomAbout` / `tlSetFps` use -- no new mechanism), forcing the bottom-timeline canvas to redraw against the new active path so its diamonds reflect the new keyframes (or no diamonds for an empty path). All three calls are `try`/`catch`-wrapped and typeof-guarded so they are safe in early init / non-author mode. Recipe-2c: the `_camSelApply` body lives strictly inside the EXISTING `T19-JS` region, the new `tlRedraw` hook on `window.__editor` strictly inside the EXISTING `T16-TRAJ` region; `template.py` byte-lock pin is **unchanged** (`_PRE_TASK20` 150691 / b9864077...). No Python/wire change. No deploy needed beyond a normal scene redeploy.
- **Spark camera-keyframe editor v2-C Phase 2 -- `#camera-select` dropdown no longer drops the original scene cameras after the first "+ Add camera".** When a scene's `viewer-config.json` ships `camera_paths` but no `cameras` (the common case for the 6 live single-path scenes), `_camSelCameras()` listed entries synthesised from the raw `camera_paths` as a fallback. After the first Add-camera, `_camSelCreate` pushes ONE new entry into `cfg.cameras`, making it non-empty -- and the old branchy `_camSelCameras()` flipped from the fallback to the cfg.cameras branch and returned ONLY that one new entry. The original scene cameras (e.g. "Fehmarn Cinematic") DISAPPEARED from the dropdown listing even though the underlying `cameraPaths` / SPCP patch still carried them (data was fine; only the dropdown view dropped them). The fix gives `_camSelCameras()` UNION semantics: cfg.cameras entries first (authoritative), then synthesised entries from EACH `cameraPaths[i]` whose `id` is NOT already covered by some `cfg.cameras[j].path_id`. Idempotent: when cfg.cameras already covers every camera_paths id, the union returns just cfg.cameras (no duplicates); when cfg.cameras is empty, the union returns the synthesised fallbacks exactly as before; the bug case (cfg.cameras has 1 new entry, pre-existing camera_paths still ship) now correctly returns BOTH. Pure: neither input array is mutated (the union is recomputed per call, consistent with the existing function shape `_camSelBuildOptions` / `_camSelInit` rely on). Recipe-2c: the change lands strictly inside the EXISTING Task-19 `T19-JS` excised region so the `template.py` byte-lock pin is unchanged (`_PRE_TASK20` 150691 / b9864077...). Zero Python/wire change.
- **Spark camera-keyframe editor v2-C Phase 2 -- critical TDZ ReferenceError at module init (every deployed scene froze) + post-startPath teardown gated on `_player`.** Two follow-up fixes to the v2-C Phase 2 "+ Create camera" change (above in `### Added`). (C1, critical) `_camSelBuildOptions` read the module-scope `const _EDITOR_AUTHOR = ModeManager.is('author')` when appending the AUTHOR-ONLY create-camera sentinel to `#camera-select` -- but `_camSelBuildOptions` is invoked from `_camSelInit()` at MODULE INIT TIME, which runs BEFORE the `_EDITOR_AUTHOR` declaration further down in the same module. Because `#camera-select` is unconditionally rendered, the read happened on EVERY page load and threw `ReferenceError: Cannot access '_EDITOR_AUTHOR' before initialization`, freezing the viewer for every deployed scene (including all 6 live ones). The fix replaces the init-time `_EDITOR_AUTHOR` read with a direct `ModeManager.is('author')` call -- `ModeManager` is a module-scope const declared well above this site, semantically identical to `_EDITOR_AUTHOR`, and safe to invoke at any time after that declaration. Phase 1 was unaffected because its `_EDITOR_AUTHOR` references in `_camSelApply` fire at USER-INTERACTION time (post-init, after the declaration is live). Pytest + the byte-lock could not catch this because they assert against the template STRING, never execute the JS. (H3, low) The Phase-2 end-user post-`startPath(val)` Resume teardown (clearing `_userInterrupted`, hiding `#user-play`) ran unconditionally; `startPath` returns without setting `_player` when the path has <2 keyframes (`buildPlayer` returns null), so the teardown silently dropped interrupt state even though no new tour started. Now gated on `if (_player) {{ ... }}`. Edge case (only reachable on a freshly-created camera with empty keyframes selected via the dropdown before Record), trivial fix. Byte-lock pin unchanged (recipe-2c, both fixes region-interior to the existing Task-19 `T19-JS` excised region). No deploy needed beyond a normal scene redeploy.
- **Spark camera-keyframe editor — Alt+Left always orbits, ~5 px non-occluding keyframe pick with click-to-cycle, and a readable keyboard hint (batch B).** Three author-mode interaction fixes, all confined to the in-viewer editor (`_VIEWER_TEMPLATE` in `viewers/spark/template.py`; end-user scenes byte-identical — byte-lock pin unchanged). (B1) **Navigation supremacy:** `Alt`+left-drag now orbits the camera no matter what is under the pointer — a keyframe frustum, the move gizmo, or a Bézier tangent handle. The capture-phase canvas handler returns at the very top on `ev.altKey` (so it never `stopPropagation()`s and OrbitControls' bubble listener receives the event), and a window-level capture guard disables both `TransformControls` instances for the gesture and restores them to their exact prior enabled state on pointerup/cancel (a capture-phase return alone cannot stop TC's own bubble pointerdown). The restore is now keyed to the initiating pointer's `pointerId` so a foreign second touch/pen releasing mid-gesture (multi-touch) cannot re-enable the controls early; a `pointercancel` or any event with no usable `pointerId` always still tears down, so the controls can never end stuck-disabled. (B2) **Fixed screen-space keyframe pick:** the pick target was a widened raycaster `Line.threshold` set to a *scene-relative metres* value (`_trajFrHalf * 0.6`), which on a large scene became a multi-metre invisible grab volume that also occluded anything behind it and made a keyframe-behind-a-keyframe unselectable. `_gzPickFrustum` now projects each keyframe pivot to screen pixels and hit-tests a **fixed ~5 px radius (~10 px on retina)** — inherently non-occluding (depth-independent) and constant at any scene scale. When several keyframes fall inside that radius, repeated clicks at ~the same screen point cycle front→behind→…→wrap (sorted nearest-camera-first); the cycle resets on selection-cleared, on trajectory rebuild (tied to the existing `_trajBuiltSig`), and when the click moves past a 6 px tolerance. (B3) **Keyboard-hint legibility:** `#controls-hint` gained a subtle semi-opaque dark rounded backing, a stronger text shadow, a higher-contrast colour, a `max-width`, and a small bottom nudge so it is clearly readable over a bright scene in author mode without crowding the merged control row — still `pointer-events:none` and still embed-hidden, so it stays unobtrusive for end users.
- **Bunny Edge Rule patterns split across ≤5-per-trigger triggers (the slug-root hardening below was rejected by the live API and never applied).** The slug-root fix added a 6th URL pattern to the Edge Rule's single trigger; Bunny enforces a hard limit of 5 PatternMatches per trigger and rejected the whole `addOrUpdate` with HTTP 400 (`{"ErrorKey":"edgerule.invalid","Message":"Maximum 5 ... per condition."}`), so the rule silently never updated and the bare slug-root URL kept serving the 30-day-stale shell — the in-memory `_desired_edge_rules` structure tests passed but did not exercise the live constraint. `_desired_edge_rules` now splits `_EDGE_URL_PATTERNS` across multiple triggers of ≤ `_MAX_PATTERNS_PER_TRIGGER` (5) each; with `TriggerMatchingType=0` (MatchAny across triggers) this is semantically identical to one combined trigger and preserves every pattern's coverage (nothing dropped). Locked by `test_deploy_extended.py::TestEdgeRuleSlugRootCoverage::test_no_trigger_exceeds_bunny_pattern_limit` (every trigger ≤5 patterns; the per-rule union equals the full pattern set) and validated against the live Bunny API by an actual successful `ensure_edge_rules` + bare-URL-freshness redeploy check.
- **Bunny Edge Rule now covers the bare slug-root URL — the real fix for the recurring stale-build-after-redeploy bug.** The no-edge-cache Edge Rule was scoped to `*/index.html` + `*/viewer-config.json`, but the URL a visitor actually opens is the bare slug *directory* (`https://splatpipe-cdn.b-cdn.net/<slug>/` and `/<slug>/?author=1&…`). Bunny matches the rule against the request URL, which contains neither `index.html` nor `viewer-config.json`, so the slug-root request fell through to the pull zone's 30-day `CacheControlMaxAgeOverride` and a redeployed slug kept serving a 30-day-stale shell unless the visitor appended a cache-buster (the recurring "I redeployed but it's still old" symptom; measured this round: bare URL `Cache-Control: public, max-age=2592000`, only `?_cb=` made it `MISS`). `_EDGE_URL_PATTERNS` now also includes `…/*/` and `…/*/?*` so the slug-root directory (with or without a query string) bypasses edge+browser cache like the explicit files; `.rad`/`.radc` URLs never end in `/` nor contain `/?` so the big immutable assets keep their fast 30-day cache, and the `addOrUpdate` Description key is unchanged so re-runs still update the same two rules in place (no duplicates). Locked by `test_deploy_extended.py::TestEdgeRuleSlugRootCoverage` (slug-root patterns present, immutable-asset patterns not broadened, Guids reused). Companion discipline (no code change needed — the CLI already deploys from a fresh process): redeploys must run via the `splatpipe` CLI / a fresh process, never an in-process `html_for` re-render, whose stale import cache can ship old viewer code even when the commit is correct.
- **`splatpipe publish` redeploys no longer clobber a scene's display name/description.** Standalone mode defaulted `--scene` to the bare slug when omitted, so re-publishing an existing slug to retune it (`publish --ply X --slug s --live s`) silently reset the page `<title>`/`og:title` and `og:description` to the slug + NEUTRAL (observed: an IBUG redeploy turned its title into "ibug"). Now, when `--scene`/`--desc` are omitted in standalone mode and the slug is already live, the real display name + description are recovered from the live `index.html` (`og:title`/`meta description`) before any default is applied; a genuinely new slug / offline still falls back to the slug name + NEUTRAL exactly as before. Best-effort, never fatal. Locked by a CLI regression test (`test_publish.py`, mocked live page). Also fixed the **CI ruff failures** introduced alongside the earlier streaming/cp1252 fixes: the `sys.stdout.reconfigure` block in `cli/main.py` sat above the relative imports (13× `E402`) and two `lambda l:` progress callbacks (`E741`) — reconfigure moved below the imports (still runs at import, before any command) and the lambdas renamed; `ruff check src/ tests/` clean again.
- **Camera-keyframe editor v2-A — bottom-chrome cleanup + anti-aliased trajectory (user feedback round).** Six coupled in-viewer fixes, no behaviour change for the six live single-camera scenes (byte-lock proven): (A1) the mid-screen transport HUD pill (`#path-hud`) is replaced for end users by ONE tiny, subtle, text-less play↔stop icon at the bottom-centre — the old `#path-select`/`#path-scrub`/`#path-time`/`#path-play`/`#path-stop` elements stay in the DOM (now CSS-hidden, all ids intact so the ~30 editor/cinematic/bench call sites keep working); (A5) prev-/next-keyframe skip buttons (`‹` `›`) flank that icon and step the cinematic to the previous/next keyframe time using the *sorted* keyframe times (correct even when keyframes are unordered in the array); (A2) the author "Show trajectory" toggle moves to bottom-right just above the timeline, and the keyboard hint (`#controls-hint`) is lifted clear of (and z-ordered above) the author bottom timeline so it is no longer occluded; (A3) the bottom-timeline transport buttons get a dedicated band above the seconds ruler (they no longer overlap the ruler); (A4) Record/interp/tangents/Save are merged onto that same transport row instead of a separate floating bar; (A6) releasing a timeline playhead/ruler scrub no longer leaves the path auto-playing (it now pauses at the scrubbed pose — the root cause was that a non-null player IS playing, with no paused state); (A7) the trajectory polyline, per-keyframe frustums and Bezier tangent lines are anti-aliased via region-interior dynamically-imported three.js fat-lines (`Line2`/`LineSegments2`/`LineMaterial`, screen-space px widths, resolution synced off the existing per-frame overlay tick — no new rAF/resize listener, graceful thin-line fallback if the addon fails to load), and the tangled dense-cluster selected-keyframe knot (the old ×1.7 in-place frustum scale) is replaced by one clean billboarded ring marker. Locked by the `template.py` byte-lock (`test_html_for_save_mode.py`): A3/A4/A6/A7 + the A2 toggle reposition are region-interior (recipe 2c, pin unchanged); A1 + the A2 hint z-fix are a deliberate, documented re-pin of intentional end-user-facing chrome.
- **Auto-focus no longer freezes after a fast move + full stop (Spark viewer "remaining chunks don't LOD in").** `_autoFocusTick` committed the camera-pose marker (`_afKey`) *before* the screen-centre raycast that places the LOD focus. Right after a fast move the centre is often over still-coarse/absent geometry, a gap, or sky, so the raycast misses and the tick returns — but `_afKey` was already advanced to the now-resting pose. Because the camera is then completely stopped, the `same pose & focus already set → skip` guard latched **permanently**: the tick never retried, `spark.lodPosOverride` stayed frozen at the last *in-motion* point, and the resting view's fine chunks were never demanded → they stayed coarse until the user nudged the camera (which changed the pose and broke the latch — hence "sometimes" and the self-heal-on-move). Fix: stamp the throttle (`_afLast`) early as before, but commit `_afKey` **only after a focus is actually applied**; a miss/degenerate return now leaves `_afKey` stale so the next tick re-tries at ~5 Hz until the rest view is hit. No behaviour change while moving or once resolved; no busy-loop (still throttled). Template-only change — live scenes pick it up on their next redeploy. A scene's public URL is `https://<cdn>/<slug>/index.html`, re-deployable in place forever. Three compounding Bunny-CDN failures that made this fragile (and blanked `/speicher/`) are fixed for good: (1) **`template.py` index.html is now build-agnostic** — it no longer hard-codes the per-build `b<hash>/scene.rad`; a new `_PRIMARY` const reads `cfg.primary_asset` from the no-store `viewer-config.json` (the always-fresh small file), falling back to the baked constant for legacy single-file deploys, so the 30-day-edge-cached shell can never point at a since-replaced subfolder. (2) A **Bunny pull-zone Edge Rule** (`OverrideCacheTime=0` + `OverrideBrowserCacheTime=0`, scoped to `*/index.html` + `*/viewer-config.json` only) makes just those two tiny text files always-fresh at the edge while the big immutable `.rad`/`.radc` keep the fast 30-day cache — the pull zone's `CacheControlMaxAgeOverride=2592000` had been overriding the client's `cache:'no-store'` too, so no-store alone was insufficient. Idempotent applier `.codex-run/bunny_edge_rules.py`; the deploy re-asserts it every run. (3) The stable-slug deploy now uses **`purge=False`**: `deploy_to_bunny(purge=True)` issued a Bunny *recursive directory DELETE* that runs asynchronously server-side and raced the immediate re-upload (eating fresh chunks and clobbering the re-PUT index/config when the build-subfolder name was stable, e.g. the `--rad-dir` content-hash key) — never delete-then-reupload the same Bunny path. Net: embed a slug once; Splatpipe rebuilds/retunes behind it with zero consumer change and zero stale-shell risk.

### Added
- **`splatpipe init-php-auth --scene <slug>` CLI (#122 Phase 1).** Provisions a per-scene PHP-save bearer token for the editor's `save_mode="http"` round-trip on a fresh CDN slug. Generates a 32-hex-char random token via `secrets.token_hex(16)`, computes its lowercase-hex `sha256`, SFTP-uploads ONLY the hash to `<remote-base>/<slug>/.author-token` on the configured host (the PHP adapter at `infra/php/save-camera.php:181-196` accepts hash-or-raw via `hash_equals`, so the raw token is NEVER at rest on the server and CANNOT be recovered from a server compromise), and prints the raw token + ready-to-bookmark `https://<cdn>/<slug>/?author=1#token=<token>` URL ONCE to the operator's console for password-manager storage. Idempotent: a second run REPLACES the prior hash (server stores only the latest); without `--force` the CLI prompts `overwrite? [y/N]` to avoid casual rotation. `--scene` slug is validated against the codebase-wide `^[a-z0-9_-]{1,64}$` charset (mirrors the PHP side exactly + blocks path traversal). SFTP creds load from `002_geddart_relaunch/.env` by default (`STRATO_HOST` / `STRATO_USER` / `STRATO_PASSWORD`), never baked in the repo, never logged. 29 new tests in `tests/test_init_php_auth_cli.py` cover token charset/randomness, sha256 correctness, slug-charset rejection (uppercase/space/dot/slash/empty/over-64), `--force` vs interactive overwrite-prompt, custom `--remote-base` / `--endpoint` / `--cdn` flags, password-never-leaks-in-output, ASCII-only status lines. SFTP layer mocked via a `_FakeSftp` recorder so no network is touched. The headline Phase 1 deliverable per `docs/superpowers/specs/2026-05-20-editor-arc-design.md` §7.9.
- **PHP save backend goes fully end-to-end on `fehmarn` (#122 Phase 1).** The PHP adapter at `infra/php/save-camera.php` gains a Bunny-CDN sync path so a viewer's authenticated Save POST now flows through the FULL round-trip end-user-visible state-change in one request. The local Strato atomic-write (the existing oracle-locked contract) is unchanged and remains the source of truth for `test_php_save_oracle.py`; the new additive `BUNNY_ENV_FILE` config (default `__DIR__/.bunny_env`, htaccess-denied) points at a `KEY=VALUE` file with Bunny Storage creds (`BUNNY_STORAGE_ZONE` / `BUNNY_STORAGE_PASSWORD` / `BUNNY_CDN_URL` / optional `BUNNY_ACCOUNT_API_KEY`). When configured, the merge now operates on the LIVE Bunny config (cURL `GET storage.bunnycdn.com/<zone>/<slug>/viewer-config.json`) instead of the local-only baseline, then PUTs the merged result back and best-effort purges the edge cache; the success response carries `bunny_push_ok` + `bunny_purge_ok` flags. When `BUNNY_ENV_FILE` is absent, the legacy local-only path runs unchanged (the oracle still skipif-gates without Bunny config). The `fehmarn` slug is now LIVE on `save_mode="http"` (deployed `index.html` bakes `SAVE_MODE = "http"` + `SAVE_ENDPOINT = "https://geddart.de/save-camera.php"`; live `viewer-config.json` carries `save_backend: {type: "http", endpoint: ...}`). End-to-end POST round-trip verified: a Save POST with the bearer token adds a `PHP Round-Trip Test` camera_path; the `viewer-config.json` on Bunny serves the new state on the very next no-cache GET, `primary_asset` invariant preserved (the locked Speicher-blank failure-class guard intact).

- **JS-side fragment param rename `#author=<secret>` -> `#token=<token>` (#122 Phase 1 Q5).** The viewer's URL-fragment bearer parser at `template_parts/17_editor_gizmo.js_tmpl::_gzAuthorSecret()` is renamed to `_gzReadAuthToken()` and reads `#token=<token>` instead of `#author=<secret>` -- this decouples the bearer name from the `?author=1` mode flag (the prior overload was confusing: the SAME word `author` meant two different things in the SAME URL). Backwards-compat: the parser still reads `#author=` as a fallback with a `console.warn` so any in-flight test bookmarks keep working until the operator rotates. The Save body shape is ALSO corrected from `{slug, patch: {...}}` (a wrapper that never matched the wire contract) to `{slug, ...patch}` (flat top-level -- mirrors what the PHP adapter actually reads via `$patch['slug']` + `foreach ($patch as $key => $value)` and what the cross-language oracle sends via `json.dumps({"slug": SLUG, **patch})`). The `gzAuthorSecret` test-surface alias is kept (now delegates to `_gzReadAuthToken()`) for any harness scripts that still reference it. Output-pin updated in lockstep across all 6 `tests/test_html_for_output_pin.py` fixtures (+1408 bytes each; the same +N delta across all 6 = clean rename + body-shape fix, no fixture-specific drift).

- **Annotation schema expanded** per user-locked Q6 (#122 Phase 5 prep): each entry of `viewer-config.json`'s `annotations[]` list gains seven optional fields on top of the existing legacy 4-tuple (`label`/`title`/`text`/`pos`). New fields: `kind` (`"dot_unfold"` | `"title3d_overlay"`, default `"dot_unfold"`), `unfold_radius_m` (default 5.0 m), `t_in` / `t_out` (timeline-animatable visibility window, defaults 0.0 / 999.0 -- the 999 floor means legacy entries stay always-on through any reasonable tour length without re-authoring), `fade_ms` (default 300), `media_url` (optional image/video URL shown in the unfolded panel, default `None`), `billboard` (always-face-camera, default `True`), `id` (auto-generated via `secrets.token_hex(4)` in the form `"ann_<8hex>"`). New helper `core.path_io.upgrade_annotation(ann: dict) -> dict` migrates a legacy entry to the fully-populated expanded shape with safe defaults at READ time (the input is never mutated; existing values pass through verbatim) so legacy 4-field entries still load byte-identically and viewer behavior is unchanged until Phase 5 ships the JS-side renderer + editor. The defaults are also exported as `core.path_io.ANNOTATION_DEFAULTS` so the Phase 5 JS-side editor can read the same source of truth. 17 new tests in `tests/test_annotation_schema.py` covering: legacy 4-field upgrade adds every new field with the locked default; minimal `pos`-only entry upgrades cleanly; already-upgraded entries pass through unchanged; idempotency under double-application; input non-mutation invariant; each documented default (`kind`=`"dot_unfold"`, `unfold_radius_m`=5.0, `t_out`=999.0 always-on, `billboard`=True, `media_url`=None); `kind="title3d_overlay"` passes through verbatim; 1000-call id-uniqueness sweep (collision check via `Counter`); id format regex `^ann_[0-9a-f]{8}$`; caller-supplied id preserved (so the existing `a1`/`a2` migration ids in `project.py` survive); full JSON round-trip preserves every expanded field; TypedDict structural acceptance of both the legacy 4-field combination AND the full expanded combination. No allow-list edit needed -- `annotations` is already on `PUBLIC_VIEWER_CONFIG_KEYS` and `ALLOWED_PATCH_KEYS`; the expansion is to the SUB-keys of each annotation entry (pure data per #108, no sub-key allow-list applied). Backwards-compat: legacy 4-field annotations still load + render -- only when Phase 5 lands the JS renderer + editor will the new fields be CONSUMED.
- New `/upload-image` web route for panorama backdrop assets (Phase 3 prep). Mirrors the #107-hardened `/upload-audio` route: triple-layer defense (char-reject + `Path.name` change-detect + `ensure_contained`), 3-extension allow-list (`.jpg`/`.jpeg`/`.png`), 5 MiB cap (Content-Length pre-check + streaming guard, partial-write cleanup on overflow), 409-reject collision (panorama is one-per-scene; user must re-upload explicitly), server-side `assets/panorama/` directory (created on first upload). 38 regression tests in `tests/test_image_upload_security.py` covering unix/windows/mixed/absolute/drive-letter traversal vectors + empty/dot/dotfile names + extension allow-list (6 accepted + 13 rejected including the R7-excluded `.hdr`/`.exr`/`.webp` + the SVG-can-carry-JS rejection) + size cap (4.99 MiB accepted + 5.01 MiB rejected with 413) + collision (409) + happy paths (JPG + PNG + sanitized response body) + sibling-prefix attack defense-in-depth + shared-helper usage guard.
- **`splatpipe build-lod --sh-encoding {auto,paged,clamped}` -- typed CLI option for SH decode-path mode (bug-audit #1 + #2).** The Spark viewer has two paged-`.rad` decode paths: the legacy `PackedSplats` path (hard-clamps SH to `±shN_max`, floored at `1.0` during build -- every live `.rad` measures `shN_max==1.0`, so it is effectively a `±1` clamp that produces the well-documented "rainbow" view-dependent artifact on SH3 captures) and the clamp-free `ExtSplats` path (fetches SH via dyno-graph float texture reads with no clamp -- runtime cost negligible, NO extra build cost because both paths share the SAME `.rad` bytes; the only knob is the per-scene `spark_render.paged_ext_splats` flag). The clamp-free path has been running on the live `kf-fehmarn` + `Fehmarn` scenes for weeks (the audit identified a 7-line uncommitted template stray at `viewers/spark/template.py:1237-1243` as both the byte-lock-failure cause in the dirty tree AND a missing first-class CLI/build/publish surface). This task formalizes the stray INTO HEAD (deliberate byte-lock re-pin, recipe-2b: `_PRE_TASK20` advances 150691 -> 151172 / sha b9864077... -> bca79294...) AND adds the missing typed CLI: new `core/sh_encoding.ShEncoding(str, Enum)` with three members (`auto` -- default, preserves inherited `spark_render.paged_ext_splats`; `paged` -- forces `True`; `clamped` -- forces `False`), Typer-native choice-gating on the CLI boundary (a typo fails LOUDLY with "Invalid value", not silently writing a wrong viewer-config), threaded through `viewers/spark/build_lod.build(sh_encoding=...)` into the cache key with a literal `e<char>` marker (`ea`/`ep`/`ec`, so re-running with a different mode never silently serves a stale entry and the marker prefix `e` never collides with the existing chunked-`c` / cluster-sh-`s` flag chars), through `steps/publish.publish_scene(sh_encoding=...)` into the staged `viewer-config.json` (`auto` preserves any inherited value -- so the 6 live single-path scenes + kf-fehmarn redeploy through unchanged; `paged`/`clamped` force the flag). Locked by 20 new tests: 8 in `tests/test_sh_encoding_cli.py` (enum invariants, all three CLI values accepted, invalid value rejected, default-is-auto, plus the resolve_paged_ext_splats `auto`/`paged`/`clamped` truth table), 5 in `tests/test_build_lod_cache_key.py` (cache namespacing under all three modes, auto-is-default, cache hit on re-run, recognisable `e<char>` marker), 7 in `tests/test_publish_sh_encoding.py` (paged sets True / clamped sets False / auto preserves inherited / auto with no base config omits the key / paged overrides inherited / default-is-auto / sh_encoding threads through `build()` for the `--ply` path). Byte-lock test (`tests/test_html_for_save_mode.py::test_defaults_are_regression_safe_existing_scenes_byte_identical`) now PASSES (was failing in the dirty tree by design). The legacy `_PRE_TASK20_*` v2-C constants are renamed to `_PRE_TASK20VC_*` (provenance-only) to avoid shadowing the new pin.
- **Camera keyframe editor v2-C Phase 3 -- delete cameras via the kebab menu, with clip-reference + last-camera guards.** Closes out the kebab actions menu landed by Phase 2 with a new AUTHOR-ONLY `Delete` item alongside `Rename`, so the dropdown collapses to a clean three-action surface (`[#camera-select] [+ Add camera] [kebab]` with `[Rename | Delete]` in the popup). Click flow: (a) close the popup, (b) two guards run -- the **clip-reference guard** refuses deletion if the camera id is bound to any clip in `cfg.clips`' cut sequence (alert: `Cannot delete "<name>" -- it's referenced by N clip(s) in the cut sequence. Remove those clips first.`), and the **last-camera guard** refuses deletion when only one camera remains (alert: `At least one camera must exist. Add another camera before deleting this one.`), (c) `window.confirm('Delete "<name>"? This cannot be undone.')`, (d) on confirm, the entry is `splice`'d out of BOTH `cfg.cameras` and `cfg.camera_paths`/`cameraPaths` (kept-in-sync mutation, mirrors `_camSelRename`'s dual-walk), (e) if the deleted camera was the saved default, `cfg.default_path_id` re-points at the FIRST remaining camera's path id (current-camera fallback rule), and (f) the dropdown is rebuilt via `_camSelBuildOptions()` then re-bound via `_camSelApply(firstRemainingId)` whose author branch fires the same `_trajForceRebuild()` + `window.__editor.gzDetach()` + `window.__editor.tlRedraw()` overlay-refresh hooks the Phase-2 rename refine relies on (no new rebind logic). The JS clip-reference walk is a faithful mirror of a new Python helper `core/scene_cuts.py::find_clips_referencing_camera(cfg, camera_id) -> list[dict]` (single source of truth -- same `Array.isArray(cfg.clips) ? cfg.clips : []` defensive gate `ClipPlayer` already uses at `template.py` ~2979; missing/non-list/empty `clips` -> 0 refs -> safe to proceed). Defense-in-depth: `_camSelDelete()` early-returns on `!ModeManager.is('author')` (the kebab is already CSS-hidden in user/embed via the existing `T12-CSS` region's `body.usermode`/`body.embed` selectors, but a devtools click in user mode is still a no-op); the choice of `ModeManager.is('author')` over the existing `_EDITOR_AUTHOR` const is deliberate -- the const lives at `template.py:4454` (after the kebab JS) and would be a TDZ ReferenceError at module init had it been a top-level call; `ModeManager` is declared at line 632 and safe everywhere after (the C1 fix lesson kept). **Zero wire change** -- `cameras` / `camera_paths` are already on the SPCP `_PATCH_KEYS` allow-list, primary_asset force-keep is untouched, and a delete-camera patch (the surviving cameras carried in BOTH lists, deleted entry STRUCTURALLY ABSENT -- not a tombstone, not a marker) round-trips through the EXISTING `merge_camera_scope` whole-replace contract byte-identically (locked by two new oracle tests in `test_config_merge.py`: `test_multicamera_delete_camera_patch_removes_entry` for the happy path and `test_multicamera_delete_camera_patch_keeps_primary_asset_invariant` belt-and-braces for the locked Bunny pointer); the JS<->Python SPCP codec round-trips the delete envelope byte-identically (new case 8 `delete-mid` in `test_spcp_js_port.py`'s `_CASES` battery, plus a pure-Python round-trip in `test_spcp_token.py::test_spcp_round_trip_handles_camera_deletion`). Recipe-2c throughout: the `<button id="cam-menu-delete">` markup lives strictly inside the EXISTING `T20-CAMSEL-DOM` region, `_camSelDelete()` + the `_camMenuDel` element grab + the click handler strictly inside the EXISTING `T19-JS` region (no new CSS needed; reuses `.quality-btn`), so the `template.py` byte-lock pin is **unchanged** (`_PRE_TASK20` 150691 / b9864077...). New `tests/test_scene_cuts.py` cases (`test_find_clips_referencing_camera_*` x4) lock the Python helper's contract; total test count rises by +8 (7 pure Python + 1 JS-port-battery case). No deploy needed beyond a normal scene redeploy.
- **Camera-keyframe editor v2-C Phase 2 REFINE -- visible "+ Add camera" / "Rename" buttons (replaces the buried dropdown sentinel).** Per user feedback, the AUTHOR-ONLY camera-creation affordance moves from a buried `+ Create camera` option at the bottom of the `#camera-select` dropdown to a VISIBLE `+ Add camera` BUTTON in the top control strip, and a sibling `Rename` button is added so authors can rename a created/imported camera in place. Both buttons reuse `class="quality-btn"` so no new CSS (only a 2-selector dual-UI gate); they sit between `#camera-select` and the Bench button, are CSS-gated to `?author=1` (invisible in default end-user / `?embed=1` views; the buttons stay in the DOM so JS wiring is unconditional and the handlers double-guard via `_EDITOR_AUTHOR`), and the dropdown returns to being purely "Perspective | each camera" (no in-dropdown sentinel). The Add button wires straight to the EXISTING `_camSelCreate()` (no change to the underlying create engine); the new `_camSelRename()` mutates `cfg.cameras[i].name` AND the matching `cfg.camera_paths[i].name` in place, then rebuilds the dropdown via `_camSelBuildOptions()` so the new label shows immediately. **Zero wire change** -- `cameras` / `camera_paths` are already on the SPCP `_PATCH_KEYS` allow-list, primary_asset force-keep is untouched, and a renamed-in-place entry round-trips through the EXISTING `merge_camera_scope` whole-replace contract with no codec or core change (locked by the new `test_multicamera_rename_round_trips_through_merge` oracle in `test_config_merge.py`). Recipe-2c throughout: the button markup lands inside the EXISTING `T20-CAMSEL-DOM` region, the JS handlers + `_camSelRename` inside the EXISTING `T19-JS` region, and the CSS gate inside the EXISTING `T12-CSS` region, so the `template.py` byte-lock pin is **unchanged** (`_PRE_TASK20` 150691 / b9864077...). Phase-3 delete is deliberately out of scope.
- **Camera-keyframe editor v2-C Phase 2 -- author-only "+ Create camera" + end-user mid-tour camera-switch fix.** The Spark viewer's `#camera-select` dropdown gains an **author-only** `+ Create camera` option (the last option, appended only when `?author=1` -- an end-user never sees or can trigger it; the selector itself stays end-user-visible from Phase 1). Choosing it builds a fresh camera-path in JS that faithfully mirrors `core/path_io.py::new_path` defaults (`id` = a `p_`+10-hex mirror of `_new_id`, `interpolation:"catmull"`, `smoothness:1.0`, `play_speed:1.0`, `loop:false`, `keyframes:[]`), registers it in BOTH `cfg.camera_paths` and `cfg.cameras` (the same `{id,name,path_id}` virtual-camera model the existing clip layer / `_camSelCameras` already consume), names it the next non-colliding "Camera N", rebuilds the dropdown (+ the hidden `#path-select`), selects it, and binds the editor to its (empty) path via the EXISTING `_camSelApply`/`selEl` rebind so Record (**K**) / the move gizmo / the timeline operate on it immediately (the spline's `buildPlayer` already guards <2 keyframes, so an empty path never throws). The authored selection persists by reusing the EXISTING `cfg.default_path_id` wire key (no new key) so the existing `_buildPatch` / SPCP token round-trip it. Also folds in the Phase-1-deferred fix: switching camera mid-tour as an end-user now clears the interrupt state and hides the stale "Resume tour" button (mirrors `_resumeTour()`'s own teardown exactly), so a fresh camera selection no longer leaves a dangling Resume control over a freshly-playing tour (normal click-to-interrupt/Resume for the default path is unchanged). **Zero Python/wire change** -- `core/spcp_token`, `core/config_merge`, `core/scene_cuts`, `core/path_io`, `set-camera-path`, the JS `_encodeSpcp`/`_spcpNum`/`_PATCH_KEYS`, and the `scene_editor.html` spline are all untouched; `primary_asset` force-keep is unaffected; the multi-camera persistence is locked by additive oracle cases in `test_spcp_js_port.py` (JS<->Python byte-identity + round-trip), `test_config_merge.py` (whole-replace + `primary_asset` force-keep), `test_scene_cuts.py` (clip refs a created camera id) and `test_set_camera_path.py` (decode+merge of a multi-camera token). The `template.py` byte-lock (`test_html_for_save_mode.py`) pin is **unchanged** (`_PRE_TASK20` 150691): all Phase-2 JS lands strictly inside the EXISTING Task-19 `T19-JS` excised region (recipe-2c absorbed, zero remainder contribution) -- no new region, no re-pin.
- **Camera-keyframe editor v2-C Phase 1 -- top-bar "Camera" dropdown (Perspective <-> animated camera).** A new `#camera-select` dropdown in the Spark viewer's top control strip (a peer of the splat-budget / move-speed / bench-mode selects, reusing `class="quality-btn"` so no new CSS; embed-hidden with the rest of `#quality-buttons`) lists **Perspective** plus every animated/scene camera and switches between free-fly and a camera's bind/playback. "Perspective" is a reserved, NON-persisted pseudo-camera sentinel (`value="__perspective__"`, always the first option, never written to `cfg` or the SPCP patch -- exactly like the existing non-persisted `idle-orbit`/`orbit` sentinels): selecting it stops any path playback (in end-user mode an explicit tour interrupt) and leaves OrbitControls/WASD in charge, with the author overlay / gizmo / bottom timeline rendering nothing via a `_trajActivePath()` Perspective guard. The dropdown is the authoritative top-bar selector that mirrors its choice into the existing hidden `#path-select` (so the ~30 transport/editor/scrub call sites are untouched); a real-camera choice in author mode binds-for-edit with no auto-play (consistent with the v2-A scrub-release-no-autoplay principle), in end-user mode drives that path's tour via the existing playback machinery. Phase 1 is selection only -- "+ Create camera" (Phase 2) and rename/delete (Phase 3) are not included. Zero Python/wire change: `core/spcp_token`, `core/config_merge`, `core/scene_cuts`, `core/path_io`, `set-camera-path`, the JS `_encodeSpcp`/`_spcpNum`/`_PATCH_KEYS`, and the `scene_editor.html` spline are all untouched; `primary_asset` force-keep is unaffected. The `template.py` byte-lock (`test_html_for_save_mode.py`) is advanced by a single new region (`T20-CAMSEL-DOM`) bounding ONLY the new `#camera-select` markup -- a deliberate, documented re-pin of intentional end-user-facing HTML (same class as the v2-A end-user shell change); the JS wiring is recipe-2c-absorbed into the existing Task-19 `T19-JS` region and the `_trajActivePath` guard into the existing Task-16 `T16-TRAJ` region, so both contribute zero to the remainder pin, and every prior region + survival assertion is preserved (the stray-bites negative control still fires).
- **Unified SceneView framework in the Spark viewer (`ModeManager` / `OverlayScene` / `InteractionManager` / `HudLayer`) — the foundation every author/cinematic feature plugs into.** Per the "structure the 3D View from the top; unify — avoid similar-but-different solutions" directive, `template.py` now has one top-level scaffold instead of scattered ad-hoc DOM/handlers: **`ModeManager`** resolves `user|author|embed` from the URL once (`?embed=1` wins, then `?author=1`, else `user`), sets a single `mode-<x>` body class, and exposes a change hook; **`OverlayScene`** is the ONE `THREE.Group` for all 3D editor/cinematic overlays with `register(layer)`/`unregister`, one mode-keyed show/hide registry, one per-frame `update(t)` fan-out and one depth/scale policy; **`InteractionManager`** is the ONE pointer/camera-ownership broker (`requestPointer(owner)`/`releasePointer`/`currentOwner`) the existing OrbitControls-vs-path-vs-look guards now route through; **`HudLayer`** is the ONE 2D-panel registry the existing settings-HUD + controls-hint register into. Exposed for tests/extension as `window.__sceneview` (`.mode`, `.overlay`, `.interaction`, `.hud`), mirroring the existing `window.__sp`/`_spDebug` convention. **This release is a pure refactor — zero behaviour change:** the `?embed=1` body still also gets the legacy `embed` class so the unchanged `body.embed …` CSS hides chrome byte-identically; the overlay group is empty (no layers yet); panels register with no `modes` so their render is untouched; `_player`/`_looking` remain the live gates (the broker only mirrors them). Verified live (Playwright): `__sceneview` surface + mode resolution + a probe overlay layer showing in `author`/hidden in `user` + `?embed=1` hiding all chrome, 0 console errors, all 466 pytest green. Adds the shared Playwright harness `tests/manual/keyframe-editor.html` (+ `splatpipe.viewers.spark._gen_harness_viewer` helper to generate/serve the viewer-under-test, Range-capable) used by this and the upcoming camera-keyframe-editor tasks.
- **Camera-keyframe editor + end-user cinematic playback shell (the feature the SceneView scaffold above was built for).** A complete in-deployed-viewer author editor and end-user cinematic experience — 19 tasks, each through an independent spec + code-quality review, with a `template.py` byte-lock test guaranteeing the live scenes' rendered HTML is unchanged. **Author mode (`?author=1`):** a viewport trajectory line + per-keyframe camera frustums + speed dots; a bottom timeline (diamonds = keyframes — drag to retime, box-select + drag an edge to scale a span, **X** deletes selected; drag the playhead for a live camera scrub; play/pause/loop; wheel zoom; optional fps grid); a click-to-select `TransformControls` keyframe gizmo (World/Screen, **R**/**T**) that edits `kf.pos`/`kf.quat` live; a 5-type interpolation popover (**V**); Record (**K**) appends a keyframe at the playhead from the live camera with the real flown pacing. **End-user mode:** intro fade-in, a multi-camera Camera-Cuts tour with next-cut LOD pre-warm (no coarse pop on hard cuts), click-to-interrupt / Resume, and a per-cut idle auto-orbit. Dual-UI gating (editor chrome for authors only; clean playback shell for end users). **Persistence (decision A, zero-backend default):** new `splatpipe set-camera-path <token>` CLI decodes the viewer-emitted `SPCP1:<slug>:<b64url>` token, camera-scope-merges it onto the scene's live `viewer-config.json` (`primary_asset` force-kept) and redeploys via the configured target; `save_mode` is `cli` (token relay, no web-facing secret) by default, with opt-in `http` (PHP + Cloudflare exemplars, each with a cross-language merge-oracle test asserting parity with the Python core). New modules: `core/spcp_token` (the wire codec; the JS port is byte-identical, locked by a Node-driven round-trip test), `core/config_merge` (the single shared camera-scope merge — allow-listed keys, `primary_asset` force-kept), `core/scene_cuts` (clip-sequence validation/ordering); pluggable `deploy_targets` (`bunny`/`folder`) and `save_backends` (`cli`/`php`/`cloudflare`) registries so scene output is no longer infra-bound; per-keyframe `interp` added to the camera-path schema (additive, applied lockstep in both viewers); a `[save_backend]` config section. Engine fixes: recording preserves the real flown pacing (not re-derived uniform timing); the path player pauses (does not teleport) on tab-backgrounding; `_autoFocusTick` yields during clip mode so multi-camera cuts no longer trigger inter-cut LoD churn. Suite: 593 collected / 567 passed, 26 skipped.
- **Clamp-free SH streaming (`spark_render.paged_ext_splats`) + `splatpipe publish --config` — fixes the prismatic "rainbow" on high-SH scenes.** Spark's default paged-`.rad` runtime decodes SH through `PackedSplats`, which hard-clamps every `f_rest` coefficient to ±`shN_max`; the `.rad` builder fits `shN_max = max(|p5|,|p95|,1.0)`, whose `.max(1.0)` floor **is** a ±1.0 clamp for any scene whose SH bulk is <1 (the common case — all 7 live scenes measured `shN_max==1.0`). On a high-frequency, view-dependent subject (a face, fabric) the clamped outlier coefficients turn into prismatic rainbow streaks that swim with the camera — absent in the trainer (Postshot, full-float). Two opt-in, **default-OFF** levers (existing scenes byte-for-byte unchanged): (1) the sibling Spark fork's `build-lod` gains `--sh-encoding=<s8|f16|f32>` — `f16`/`f32` store SH clamp-free (the `RadShEncoding::F16` path existed in the fork but was unreachable from the CLI; `resolve_sh_encoding` early-returns for a non-Auto encoding, so it also skips the percentile/floor fit); (2) `template.py` reads `spark_render.paged_ext_splats` → `sparkOpts.pagedExtSplats = true`, routing the paged decode through the clamp-free shared-exponent `ExtSplats` path instead of `PackedSplats`. An F16 `.rad` needs **both** (the file must be clamp-free AND the runtime must not re-clamp it). `cli/publish_cmd.py` gains standalone **`--config <json>`** to carry an authored viewer-config (start_view / camera_paths / default_path_id / spark_render) through `publish` the same way project mode inherits `scene_config` (mutually exclusive with `--live`). First shipped as the new permanent scene **`/fehmarn/`** — an F16 paged-streamed scene with an auto-playing outside→interior→face→orbit cinematic, verified pristine at the captured pose vs the Postshot reference. Note: F16 ≈ doubles the SH payload (per-splat, no codebook), so it is for scenes that need full clamp-free SH without cluster-sh; large cluster-sh scenes should instead use `--cluster-sh --sh-encoding=f16` (F16 only the ≤64K codebook — clamp-free at ~zero size cost), to be verified before any re-roll.
- **`splatpipe publish` — first-class CLI command for permanent-slug scene deploys.** The proven `.codex-run/deploy_scene_cs.py` + `bunny_edge_rules.py` scratch tooling is promoted into the package and **deleted** from `.codex-run/` (no shims). New `src/splatpipe/steps/publish.py::publish_scene()` (a `ProgressEvent` generator returning `StepResult`, like `deploy_to_bunny`) composes the existing library: build (or stage a prebuilt `--rad-dir`) → immutable `b<key>/` chunk subfolder → inherited+overridden `viewer-config.json` carrying the `primary_asset` pointer → build-agnostic `index.html` (7 self-check assertions) → edge-rule → `deploy_to_bunny(purge=False)` → selective 2-file purge → opt-in stale-subfolder prune. The Bunny Edge-Rule applier and Storage-subfolder listing move into `steps/deploy.py` as reusable `ensure_edge_rules()` / `list_bunny_subfolders()` (so `set-start-view`, `export` and `publish` share one Bunny layer). `cli/publish_cmd.py` is **dual-mode**: `splatpipe publish -p <project>` (source = the project's reviewed lod0 PLY, config = its `scene_config`, slug = `cdn_name`, records a new `publish` step in `state.json`) **or** standalone `splatpipe publish --ply X --slug s` / `--rad-dir` (with `--live` to inherit a deployed slug's config). Flags: `--scene --clip-xy --move-speed-mult --splat-budget --crop-within --desc --prune-stale --spark-repo --env`. `STEP_PUBLISH` added to `core/constants.py` (+ step description). The hard-won load-bearing invariants — build-agnostic index (no `b<key>` baked in), `cfg.primary_asset` pointer, `purge=False`, edge-rule asserted, only the 2 text files purged — are locked by `tests/test_publish.py` (6 tests, against the real `html_for` template, not a mock). `template.py`/`html_for` were treated as immutable (already parameter-complete). Net: a portfolio embeds a `https://<cdn>/<slug>/index.html` URL once; `splatpipe publish` rebuilds/retunes behind it forever with zero consumer change. All `publish` CLI + progress output is ASCII-only (`->` not `→`, `-` not `—`) so it never crashes a cp1252 Windows console when run non-interactively / piped / in the background.
- **Auto view-tracking focus (Spark viewer).** The viewer continuously drives `spark.lodPosOverride`/`lodQuatOverride` to a virtual pose ~10% of the hit distance off whatever the screen centre is looking at (raycast the centre each ~180 ms when the camera moved), so the LoD selects the fine leaves for that region even at a far framing — the only mechanism that beats the distance-vs-LoD ceiling (a far camera never selects sub-pixel leaves no matter the budget/lodRenderScale/lodSplatScale/finer build — all exhaustively disproven). No gesture: turn the camera and the new centre sharpens itself ~0.2 s later. Gated to after preload (so it doesn't fight the preload orbit-walk), off during camera-path playback / bench / `?stock=1`; toggle at runtime with **F**. Paired with a **moderate cone foveation default** (`coneFov0 55 / coneFov 110 / coneFoveate 0.5 / behindFoveate 0.25`, non-STOCK; per-scene config and `?coneFov0=` still override) so the centre is crisp while edges stay soft-but-acceptable rather than the aggressive-global mush.
- **Toggleable on-screen settings HUD** — press **H** in any Spark viewer to show budget / active splats / fps / focus state / lodRenderScale / device tier. Off by default; replaces the ad-hoc Playwright-injected overlay (also self-labels screenshots).
- **`?bench=probe` — teleport/load benchmark with adaptive per-pose load-in measurement.** A new bench mode that teleports the camera through a list of authored poses (`viewer-config.json` → `probe_views`: `[{pos[3], quat[4], fov}]`, e.g. captured via "Set start view" at points of interest) and, at each pose, **waits until streaming actually settles** rather than holding a fixed dwell — the question being answered is "how long until all important detail is in", so a fixed timeout would have measured the wrong thing. "Settled" means VISUALLY converged, gated on true **quiescence** — `splats` *count* hitting the budget is the wrong proxy because Spark refines coarse→fine after a teleport and that swap is roughly count-neutral, so the count plateaus while the image is still sharpening (user-reported "it moves on although the image is all blurry"). The gate requires, held for `SETTLE_WIN_MS` (1500 ms): zero active fetchers **and** zero decode backlog (`pager.fetched`) **and** working-set size (`fetchPriority.length`) **and** resident-page count **and** active-splat count all unchanged — i.e. nothing in flight and the LoD selection has stopped moving. (`spark.current.mappingVersion` was tried as the gate but the v14 contact sheet disproved it — it keeps ticking on minor re-map/re-sort even when the image is visually static, so every pose hit `MAX_WAIT` while fully sharp; it is now reported informationally only, never gated on. This is exactly why the built-in screenshots exist: ground truth corrected the heuristic.) `MIN_LATENCY_MS` (400 ms) post-teleport floor blocks the pre-reaction "looks quiet" false-positive; `MAX_WAIT_MS` (16 s) cap so a pose that never resolves (e.g. phone where the LoD working set exceeds the page pool) records `settled:false` and the run continues. `fetchPriority.length` is used only as a *stability* signal, never a drain signal — it is the desired LoD working-set size and stays ~150–200 (desktop) / ~76 (phone) even when fully loaded (verified from real device traces), so a "queue→0" test would never fire. **Built-in per-pose screenshots:** the canvas is grabbed at each settled pose (synchronously inside the render loop right after `renderer.render()`, the only way to read valid pixels with `preserveDrawingBuffer:false`) and assembled into one downloaded **contact-sheet JPEG** — a labelled grid (pose # · load-in ms · splats, green=settled / red=timed-out) so the user sees exactly what "loaded" looked like at every artwork, independent of the heuristic. Each pose is pinned by a constant 2-keyframe hold player, so the mode inherits every robust-bench guarantee (camera ownership via `_player`, the `!_player`-gated `controls.update()` clobber-fix, the monotonic recorder, the camera-motion self-check, `window.__benchResult` / `bench:done`). Per-pose results (`loadInMs`, `settled`, `splats`, `pages`) are written to `_benchTrace.probe.perPose` in the downloaded JSON, mirrored to `window.__probeLoadIn` and `window.__benchResult.probe`, and `console.table`-d. Falls back to a 6-pose orbit-ring around the start view when no `probe_views` are configured, so it is never empty. `probe_views` is read but never written by the viewer (static-file constraint preserved).
- **`?bench=rotate` — frame-times-while-loading benchmark (bench #2).** Where `?bench=probe` answers "how long until loaded" (static), `rotate` answers "how janky is it while it loads as you move": it teleports to each `probe_view` and then oscillates the look ±55° around world-Y for 6 s **while the new off-axis detail streams in**, recording per-frame `dt` + longtasks + `fetchersActive`. Per-pose it reports `fps_mean`, `dt_p50/p95/p99/max`, `longtasks`, and `jankWhileFetching` (frames >50 ms *while fetchers were active* — the actual stutter-during-load metric, e.g. the M1 14 fps / 82-longtask case). The rotation path is `loop:true` with `θ=ROT_DEG·sin(2π·f)` so start==end (seamless loop, never auto-`stopPath()`s between poses). Reuses the robust recorder, the in-render-loop screenshot capture and the contact-sheet builder (`_probeSheet` generalised with an optional label formatter + `kind` for the filename) — the sheet tile turns green when `fps_mean≥50 && p99<100 ms`. Results in `_benchTrace.rotate.{fps_mean,dt_p99_max,longtasks_total,jankWhileFetching_total,perPose}`, `window.__rotateStats`, `window.__benchResult.rotate`, `console.table`. Same `probe_views` source + orbit-ring fallback; shares `_benchStop` / camera self-check (`rotate` registered as a moving mode).
- **Bench modes exposed in the viewport.** A `bench-mode` `<select>` (next to the Bench button, same styled-control pattern as the splat-budget dropdown) lets any viewer launch Orbit / Probe (load time) / Rotate (fps while loading) / Dolly-in / Cold load without a URL param — both new load benches are now one click away in-app. The dropdown is pre-synced to `?bench=` (deep links still work and stay consistent), and the Bench button runs whatever is selected; `?bench=1` still forces the static recorder.
- **Safari-on-Mac streaming hint (Spark viewer) — the shipped mitigation for the ANGLE-Metal jitter (#78).** A small, dismissible in-viewer notice ("For the smoothest playback on Mac, open this scene in **Safari** — other browsers can stutter while detail streams in"). Shown **only** when the live WebGL renderer string contains both `ANGLE` and `Metal` — i.e. a Chromium/Firefox browser on a Mac, the *exact* discriminator that proved the unfixable synchronous ANGLE→Metal staged-upload jitter (Safari → `Apple GPU`, no ANGLE; Windows/Linux ANGLE → no `Metal`; iOS → all WebKit, no ANGLE — every non-affected platform is correctly excluded). A conservative UA fallback (Mac-desktop + non-Safari engine) covers only the case where the renderer string is masked (Brave strict shields / Firefox RFP). Dismissal is remembered via `localStorage` (no nagging every load); `?stock=1` opts out; `?safariHint=1|0` forces it on/off for verification on any machine. Reuses the renderer string the Apple-Silicon profile already captured (no extra GL context). This is the conclusion of the exhaustive #78 root-cause: the jitter is intrinsic to ANGLE-Metal on Apple Silicon and unfixable from the app or via any flag (3 fork upload rewrites + WebGPU + ANGLE's own `alwaysPreferStagedTextureUploads` off-switch all measured ~83–86 longtasks; Safari native = 0) — so the honest fix is to point Mac visitors at the browser that *is* smooth.
- **Shareable link preview cards (Open Graph + Twitter) for the Spark viewer.** Pasting a deployed viewer URL into Telegram / WhatsApp / iMessage / Discord / Slack / Twitter / LinkedIn now unfurls a rich card (title · description · large image) instead of a bare link. `template.py`'s `html_for()` gained optional `share_url`, `share_image`, and `description` kwargs and emits a `{share_meta}` block of `og:*` + `twitter:card=summary_large_image` + `<meta name=description>` tags in `<head>` (all dynamic text HTML-escaped; the joined block is substituted as one `.format()` field so no brace-doubling and no injection risk). Fully backward-compatible — every kwarg is optional, so `SparkAssembler` and older deploy scripts keep working and still get a title/description card; `share_image` defaults to a relative `preview.jpg` (a scraper resolves it against the page URL) while deploy scripts pass an **absolute** Bunny URL for maximum cross-platform reliability, and `og:url` is emitted only when `share_url` is given. The card image is a 1200×630 screenshot of the scene at its start view, generated and uploaded as `preview.jpg` next to `index.html` by a new reusable `.codex-run/make_share_preview.py` (Playwright loads the deployed viewer, hides the HUD/hint/loading chrome, captures, uploads + purges) so every scene — including the #57 redeploys — gets a card automatically with no backend (fits the static-CDN constraint).
- **`?embed=1` — clean canvas-only mode for `<iframe>` embedding.** Appending `?embed=1` to any deployed viewer URL hides every Splatpipe chrome element (header, splat-budget/bench buttons, stats readout, controls hint, Safari hint, camera-path HUD) via a `body.embed` CSS class, leaving just the 3D scene, annotations and camera-path playback — so a portfolio/site can drop the viewer into an `<iframe>` without the authoring UI showing. Pure additive opt-in (default off → zero change to normal viewing); CSS-driven so there's no per-element JS or layout flash; the splat/scene behaviour is identical. Ships automatically on every scene built from the current template (the in-flight #57 redeploys included). Paired with the share-card `preview.jpg` it doubles as the iframe's lazy-load poster. Embedding contract documented for the consuming site in a self-contained handoff.
- **Per-scene WASD/fly camera-speed multiplier (`spark_render.move_speed_mult`) + `?moveSpeed=N` live knob.** Fly speed is `max(2, _initDist·0.6)` where `_initDist` is the **authored start-view camera→pivot distance** — a framing artifact, not true scene size, so a scene whose start view sits far from its pivot (e.g. Polygraf) flies ~2× too fast even though it's the same physical scale. Rather than rescale the global heuristic (which would slow every other scene), `move_speed_mult` (added to `Project.DEFAULT_SCENE_CONFIG.spark_render` and the viewer `_DEFAULTS`, **default 1.0 = unchanged for every existing scene, zero regression**) multiplies the base speed per scene, read from `viewer-config.json`; `?moveSpeed=N` overrides it live for A/B feel-tuning. Same proven per-scene-config + URL-knob pattern as `clip_xy`. `.codex-run/deploy_scene_cs.py` gains `--move-speed-mult` to bake it at deploy (Polygraf shipped at `0.5`). Also fixes the misleading earlier audit note that called the speed "scene-extent adaptive" — it is start-view-distance adaptive.
- **In-viewer live "Speed" control (no redeploy ever) + per-scene splat budget honored.** Two follow-ups so scenes are tunable without rebaking the 441 MB asset: (1) a **Speed dropdown** in the quality bar (`0.1×…4×`) that changes the fly multiplier *live* and persists per-device in `localStorage` — so the camera feel is adjusted in the UI by the user/visitor with zero reload or redeploy; the camera-speed const became a live `let` driven by it, resolution order URL `?moveSpeed=` ▸ localStorage ▸ per-scene `move_speed_mult` ▸ 1.0. (2) `pickDefaultBudget()` now honors a per-scene top-level `splat_budget` (was a dead `_DEFAULTS` key) on a **capable discrete-GPU desktop only** — phones / tablets / Apple-Silicon keep their measured-safe tier value so a per-scene bump never tanks a constrained device; the runtime dropdown + `?budget=` still override. Fixes Polygraf looking muddy at its wide aerial start view (user-confirmed it resolves at ~3 M; a large-extent capture spreads the default 2 M budget too thin) — `splat_budget: 3_000_000` baked per-scene, config-only, no scene rebuild. `.codex-run/deploy_scene_cs.py` gains `--splat-budget`. Recorded cluster-sh k-means convergence per scene (`docs/cluster_sh_convergence.md`) + per-scene build/deploy stats (`docs/scene_stats.md`): the codebook converges by ~iter 5 (Polygraf) / ~iter 2-3 (Fabrik) — 10 iters is overkill, ~6 is visually identical, so the heavy unbuilt scenes can use `--cluster-sh=6` for ~40 % less clustering time at no perceptible loss.

### Changed
- **Spark desktop default budget 1.5 M → 2 M** (phone 500 K / tablet 1 M unchanged). With auto-focus concentrating the budget on the looked-at region, 2 M is enough to keep the centre sharp on an M1-Pro-class machine (validated: 2 M + focus ≈ 6 M + focus on the IBUG wall, 113 fps).
- **Spark 2 root-chunk pin replaced with a page-eviction guard — fixes slow detail streaming.** The viewer kept coarse coverage resident (so a not-yet-streamed region never renders as a blank gap) by re-prepending 16 root chunks to the *front* of the pager's `fetchPriority` on **every** `driveFetchers()` call. With only 3–4 fetch slots that made fine, camera-relevant chunks queue behind 16 coarse ones every frame, so detail "loaded in" slowly and LoD transitions crawled visibly ("reshading"). Verified against `spark/src/SplatPager.ts`: the new approach (a) queues a still-missing root chunk once, appended at the *end* so Spark's own camera-priority ordering keeps full priority + all fetch slots for in-view detail, and (b) filters root-chunk pages out of `freeablePages` after the real `driveFetchers` so `allocateFreeable()` can never evict them — the actual anti-disappear guarantee, at zero fetch-priority cost. Measured: cold-start preload ~30% faster (Speicher 2193 ms vs ~3100 ms; Stettiner 3201 ms vs ~3300–3900 ms). Regression-checked: no splat-disappear under aggressive orbit + deep zoom on Speicher (the disappear-prone scene) and Stettiner (30 M, the largest); 0 console errors. Also corrected the false "Spark numLodFetchers hard cap is 4" code comment (that is the decode-worker pool; fetch concurrency is not capped).
- **Spark 2 `numLodFetchers` raised and made tier-aware: desktop 8, tablet 6, phone 3** (was 4 / 4 / 2). With the per-frame coarse-chunk re-prepend gone (above), the fetch slots are no longer wasted re-confirming resident roots, so more parallel HTTP-Range fetchers directly hide CDN round-trip latency and stream fine detail in faster. Decode is still bounded by Spark's fixed 4-worker pool (hence diminishing returns past ~8). Corrected a false code comment that claimed Spark's `numLodFetchers` is hard-capped at 4 — verified in `spark/src/SplatPager.ts` that the default is 3 and uncapped; the 4 is the decode pool. Regression-checked on Speicher + Stettiner (30 M): no splat-disappear under aggressive orbit/zoom, 0 console errors.
- **Spark 2 desktop `lodRenderScale` lowered to `0.75`** (from Spark's default `1.0`; phone/tablet keep `1.3`), plus a new `?lodRenderScale=N` URL override (highest priority, same spirit as `?budget=`). Rationale: at the default `1.0` the LoD never subdivides into sub-pixel splats, so during close/mid navigation the visible count plateaus below the chosen budget even when finer detail exists; `0.75` lets it climb toward the budget there. Measured caveat (verified on Speicher, RTX desktop): at wide *aerial* framings this changes nothing — the visible count is bounded by how much geometry is on screen (~4.2 M for Speicher's spawn view), not by the detail floor, so no LoD knob raises it without moving the camera closer. It is not a bug that a 6 M budget shows ~4 M on a wide shot. Sub-pixel splats cost some fragment work; desktop-only, opt-out via `?lodRenderScale=1` or `?stock=1`.
- **Spark 2 viewer now uses a static per-device splat budget — no adaptive FPS controller.** The runtime `lodSplatScale` loop (refresh-rate detection → vote-based up/down scaling) was removed: research confirmed no shipping splat viewer uses an adaptive FPS loop, and the runtime `· NNN%` / `frozen` readout was confusing without buying real quality. `spark.lodSplatCount` is now picked once from the device tier and held: **phone 500 K, tablet 1 M, desktop 1.5 M** (measured-comfort values — iPhone 13 mini ≈ 40 fps, M1 Pro ≈ 30 fps, discrete GPUs 60 fps; the dropdown still overrides at runtime). The HUD is simplified to `Splats: X.XM · NN fps`. `maxPagedSplats` (total resident pool) is capped at **6 M** on desktop/tablet (was 32 M on desktop — needless memory for a 1.5 M visible budget) and stays at 4 M on phone for iOS canvas-memory headroom. The runtime budget dropdown is trimmed to match the pool — `500K · 1M · 1.5M · 2M · 3M · 4M · 6M` (the old `8M–23M` and `No limit` options offered counts the 6 M pool can never satisfy, which is exactly the kind of confusing knob this change set out to remove; `?budget=N` still works as a power-user escape hatch). Bench mode, orbit prefetch, the preload bar and the mobile knob set are unchanged. The `?adaptive=` and `?targetFps=` URL params are gone (`?budget=N`, `?stock=1`, `?preload=0`, `?bench=…` remain). All five live Spark viewers (IBUG, Speicher, Stettiner, Fabrik, Polygraf) redeployed.
- **PlayCanvas CDN bumped from `2.17.0` to `2.18.1`** in all three viewer surfaces — the embedded `_VIEWER_TEMPLATE` in `lod_assembly.py` (assembled-output `index.html`), the static dashboard preview `web/static/viewer.html`, and the scene editor `web/templates/scene_editor.html`. v2.18.0 was a major GSplat refactor (50+ commits): compute-based tiled WebGPU GSplat renderer, GPU radix-sort optimisations, always-on frustum culling moved into compute, vectorised Gaussian eval across 2×2 pixel quads, per-node SH color updates with angle threshold, and a new `GSplatParams.renderer` enum. The 2.17.1 / 2.17.2 update-loop regression that originally pinned us to 2.17.0-exact is gone — verified by running both versions of the same Gutsmutstrasse splat side-by-side (`tests/manual/pc-compare.html`); 2.18.1 renders 2.99 M splats with zero console errors. Also fixed a pre-existing inconsistency where `viewer.html` and `scene_editor.html` had pinned the main `playcanvas` import but loaded `camera-controls.mjs` from an unpinned `playcanvas/scripts/...` URL — both are now pinned to `@2.18.1` in lockstep.
- **`@playcanvas/splat-transform` bumped to `^2.0.4`** (was `^1.10.2`). v2.0 refactored progress reporting (PR #204) — the v1.x `[1/8] Generating morton order` per-chunk step labels are gone, replaced by a `▸`-prefixed structured tree. The `lod_assembly` progress parser now matches the v2.x chunk lines (`▸ [N/M] X_Y` inside the Writing section) via `_CHUNK_RE`, with `_SECTION_RE` capturing top-level section labels (Reading / Writing / Filter NaN) for the user-visible step message. Both the streaming and non-streaming command builders now pass `--no-tty` so the new logger emits stable, line-by-line output suitable for subprocess piping. Captured-stderr unit tests in `test_lod_assembly.py::TestProgressRegex` lock the parser against the v2.x format.
- **`pyproject.toml` `[tool.ruff] target-version`** updated from `py311` to `py312` to match the Python floor bumped in v0.7.0.
- **`--rad-chunked` is now the Spark 2 pipeline default.** `build_lod.build()` defaults to `chunked=True`: build-lod emits a small manifest `<stem>-lod.rad` plus N sibling `<stem>-lod-<i>.radc` chunk files instead of one monolithic `.rad`. The set is cached as a *directory* under `~/.cache/splatpipe/rad/<key>/` — staged via a `.tmp` sibling then atomically swapped, so a crashed build never leaves a half-written cache that the new `_find_manifest()` completeness guard would wrongly serve; `build()` returns the manifest path. `SparkAssembler` copies the manifest as `scene.rad` and every `.radc` with its **original basename** (the manifest references chunks by basename, resolved by the viewer relative to the manifest's own URL — no viewer or `PRIMARY_ASSET` change needed). `deploy.py` already handled the set (`rglob` upload + a pre-existing `.radc` → `max-age=31536000, immutable` rule + purge). Why: hundreds of independently CDN-cacheable objects (IBUG = 477) kill the cold-edge Range-miss, parallelise over the fetchers + HTTP/2, and let repeat visitors hit warm per-file edge cache — measured ~1.5× faster first paint. `splatpipe build-lod` gains `--chunked/--no-chunked` (default chunked) and now reports total size + chunk count instead of the tiny manifest's size. The legacy single-file path is fully preserved via `chunked=False` / `--no-chunked`. New `tests/test_spark_chunked.py` (9 tests) locks the cache-completeness guard and the chunked/non-chunked cache layout + key isolation.
- **`--cluster-sh` is now the Spark build default (large-scene pipeline productised).** This whole session converged on one way to ship large Gaussian scenes; it is now the splatpipe default, not a one-off `.codex-run` script. `build_lod.build()` gained `cluster_sh: bool = True` — it passes build-lod `--cluster-sh` (SH coefficients vector-quantised into a ≤64K codebook): empirically **~60% smaller `.rad`** (IBUG 1653 MB → 661 MB) at no perceptible quality loss and far less GPU/JS memory, which is what makes 3–7 GB source PLYs deployable at all. `SparkAssembler` now builds cluster-sh by default; `splatpipe build-lod` gains `--cluster-sh/--no-cluster-sh` (default on). The flag set (`q`/`n` + `c` + `s` + extra) is part of the build cache key so flipping it rebuilds correctly rather than serving a stale asset. **Hard dependency, also already default:** cluster-sh `.rad`s only run on the patched self-hosted Spark fork the viewer template pins via `SPARK_FORK_URL` (rcf2 — the `ChunkDecoder` RefCell-reentrancy fix + the chunk-0 SH-codebook ordering gate; upstream `@sparkjsdev/spark@2.0.0` panics/OOMs on every cluster-sh chunk). `--no-cluster-sh` produces a larger, stock-Spark-compatible build for anyone not using the fork. Reusable `.codex-run/deploy_scene_cs.py` builds+deploys any source PLY through this default pipeline (chunked cluster-sh on the final template: share-card, per-scene clip_xy, #81, rcf2) to a fresh Bunny path.
- **Spark `clipXY` is now per-scene config, default lowered 3.0 → 1.4 (Phase 1 #6).** `clipXY` (per-splat XY frustum-cull slack) was a hard-coded global `3.0` in `template.py` — a margin only a few scenes actually need (it was bumped globally solely because Speicher's `.rad` has giant outlier splats, ln-scale ≈9, that Spark's default 1.4 culls when their centres go off-screen → "scene goes blank"). The wide margin is pure fragment cost for every other scene. Now `spark_render.clip_xy` (added to `Project.DEFAULT_SCENE_CONFIG.spark_render` and the viewer `_DEFAULTS`, default **1.4** = Spark's own default) is read per scene from `viewer-config.json`, with a new `?clipXY=N` URL override joining the existing A/B detail-lever knobs. **Migration:** a scene that genuinely needs the wide margin (Speicher, and any other with training-outlier splats) must carry `spark_render.clip_xy: 3.0` in its own `viewer-config.json` / project `scene_config`; this is injected at that scene's redeploy. Scenes redeployed without it use 1.4 — verified on IBUG (no blanking under aggressive orbit/zoom) before shipping.

### Added
- **"Set start view" — author a Spark scene's opening camera from inside the deployed viewer.** New header button captures the live camera (position, rotation, FOV, orbit target), shows a confirm dialog, then emits a compact `SPV1:<project>:<base64url>` token (copied to clipboard + shown selectable). Because the viewers are static files on Bunny with no backend (and the storage key must never be in client JS), persistence is a relay: the user sends the token to the assistant, who runs the new **`splatpipe set-start-view "<token>"`** CLI. That decodes/validates it and writes a `start_view` field into the project's `viewer-config.json` on Bunny (+ CDN purge; and into the local project's `scene_config` when `--project-path` is given, so re-exports keep it). The viewer treats `start_view` as the **highest-priority** initial camera — above camera-paths, annotations and the auto sqrt-distance fallback — and it seeds the Reset/Home + orbit-bench origin too. `start_view` added to `Project.DEFAULT_SCENE_CONFIG`. Verified end-to-end on Bunny (button → token → CLI → reload spawns at the saved pose). Design: `docs/plans/2026-05-15_set-start-view.md`.
- **Fixed: `viewer-config.json` is now fetched with `cache: 'no-store'`.** Bunny's pull zone serves it with a 30-day `max-age` (its default override; "Honor Origin Cache-Control" is off so `.rad` stays long-cached), so a browser that had already opened a viewer kept a stale scene config for weeks — which would have silently broken "saved for all" for `start_view` and any scene-config edit. The file is a few KB; always revalidating it is free and makes config changes appear on the next load.
- **Manual mobile test harness for PlayCanvas bumps** under `tests/manual/`: `pc-2-18-1.html` is the deployed Gutsmutstrasse viewer with the playcanvas pin swapped, and `pc-compare.html` is a split-pane shell (vertical on phones, side-by-side on landscape ≥800px) iframing the production viewer next to the test page. Designed so the next PlayCanvas bump is a 30-second version-string edit + Bunny re-upload, not a fresh detective story. Lesson learned the hard way: PlayCanvas resolves chunk URLs from `lod-meta.json` relative to the *page URL*, not `<base href>`, so the test page has to live in the same folder as the chunks.
- **LichtFeld PPISP train option in the project UI.** The Train Splats settings now show Photometric Compensation: it is enabled for LichtFeld projects and maps to the existing `--ppisp` trainer flag, while Postshot projects show the control as unavailable because Postshot CLI v1.0.359 does not expose a photometric-compensation flag.

### Fixed
- **`--cluster-sh` Spark builds no longer crash the viewer — patched, self-hosted Spark fork.** Upstream `@sparkjsdev/spark@2.0.0` panics `RefCell already borrowed` in `ChunkDecoder::push` (`rust/spark-worker-rs/src/decoder.rs`) on every chunk of a `--cluster-sh` build: the function held a `borrow_mut` on the `BUFFER` thread-local `RefCell<Vec<u8>>` across the `self.receiver.push()` callback, and the cluster-sh receiver path (SH-codebook + `sh_label` expansion) re-enters that same thread-local — the nested borrow panics, aborting the WASM module, which the browser surfaces as a generic "Out of Memory" page even with tens of GB of RAM/VRAM free. Plain (non-cluster) chunked builds never re-enter, which is why only `--cluster-sh` crashed despite being ~60% smaller (IBUG 661 MB vs 1653 MB). Forked at `H:/001_ProjectCache/1000_Coding/spark` (branch `fix/chunkdecoder-refcell-reentrancy`): `mem::take` the scratch `Vec` out of the `RefCell` so it is not borrowed while the receiver runs, then restore it for reuse — preserves the per-thread buffer-reuse optimisation, zero decode-behaviour change. The patched production ESM bundle (`vite --mode production` → `spark.module.min.js`, WASM base64-inlined, `three` left as a bare import resolved by the page importmap) is self-hosted on Bunny at a version-pinned, never-reused path (`/_sparkfork-rcf1/spark.module.min.js`; bump `-rcfN` per fork change for edge-cache safety). `template.py` gains a `SPARK_FORK_URL` constant and repoints the `@sparkjsdev/spark` importmap entry off jsdelivr to the fork — Spark is now same-origin with the viewer HTML + `.rad`/`.radc` (connection already warm); only `three` still preconnects to jsdelivr, and the preconnect comment is corrected accordingly. Root cause was captured live from the failing viewer's console via Playwright after several disproven memory/VRAM/V8-2 GiB-cap theories — the actual fault is a Rust re-entrancy panic, not an allocation failure.
- **`--cluster-sh` second abort fixed — chunk-0 SH-codebook ordering gate.** Even with the RefCell fix above, `IBUG_cs_v2` still aborted ~7×/load with `panicked at spark-worker-rs/src/packed_splats.rs:695: index out of bounds: the len is 0 but the index is N` — caught live via Playwright during the Phase 0 measurement pass (exactly why measurement precedes optimisation). Root cause: `--cluster-sh` carries the SH codebook only in chunk 0, but nothing sequences chunk 0 ahead of chunks 1..N; a non-zero chunk whose decode worker resolves before chunk 0's reads `this.sh1Codes/sh2Codes/sh3Codes` while still `undefined`, so Rust `set_sh_labels` indexes an empty codebook `Vec` and the WASM worker panics. Non-deterministic (depends on fetch/decode scheduling) — which is why it "felt better but wasn't fixed." Fix (`SplatPager.ts`, same fork branch): a per-`PagedSplats` `codebookReady` promise; `fetchDecodeChunk` detects a clustered build via the new `RadMeta.shCodeCount` and, for any `chunk !== 0`, awaits chunk 0's codebook before dispatching the worker decode. Plain (non-cluster) chunked builds have `shCodeCount` absent → gate skipped → zero behaviour change or regression. TS-only (no WASM rebuild; the RefCell-patched WASM is preserved). Self-hosted bundle bumped `_sparkfork-rcf1` → `_sparkfork-rcf2`.
- **Desktop Spark refetch storm fixed — page pool now covers the LoD working set.** Phase-0 instrumentation (Playwright on IBUG_cs_v3, 2026-05-16) found the real "detail loads slow / cluster-sh not faster" cause: `SplatPager.driveFetchers` evicts and **re-decodes** every chunk the per-frame-rebuilt `fetchPriority` lists beyond `maxPages`. At the 2 M desktop budget the LoD traversal selects ~182 chunks but `maxPagedSplats=6 M` gave only 92 pages, so ~90 chunks were perpetually evicted and re-decoded in the 4-worker WASM pool — **~53–79 fetch+decode/s with a *static* camera, 14× refetch over a 477-chunk scene** (`fetched`-backlog 0, fetchers avg 2.1/8 → neither decode-pool- nor fetch-bound; pure evict/re-decode thrash). Controlled live A/B (lodSplatCount sweep): 0.5 M → 79 chunks ≤ 92 → 0.3/s (storm gone); 1 M → 115; 1.5 M → 155; 2 M → 182 ≫ 92 → 79/s; `lodRenderScale` had **no** effect on working-set size (refuting a prior hypothesis). Fix (`template.py`): desktop `maxPagedSplats` 6 M → 16 M (92 → 244 pages, ~1.34× the static working set + camera-movement turnover headroom); phone stays 4 M (iPhone Safari 256 MB ceiling) and tablet 6 M (follow-up tracked) so neither memory profile regresses. Largest SH ArrayBuffer at 244 pages ≈ 244 MB, far under V8's ~2 GiB cap. No quality change (budget unchanged at 2 M). The prior "6 M is plenty" cap (2026-05-15) predated this measurement; this supersedes it on evidence.
- **Apple-Silicon (M1/M2) desktop profile + Bench-button fix.** A no-touch MacBook is classed `desktop` and got the discrete-GPU profile, but a real M1 Pro bench trace (user/Playwright, 2026-05-16) showed it **GPU-fill-bound**: ~82 ms GPU/frame rasterising 2 M splats at Retina DPR 2 → ~24 fps, while streaming/pool were perfectly healthy (`pagesResident` stable, 0 storm — the page-pool fix works there too; this is pure rasterisation, not memory or streaming). `template.py` now detects Apple Silicon from the GL `UNMASKED_RENDERER` string and applies a lighter fill profile **gated to Apple Silicon only** (discrete-GPU desktops, phones, tablets are byte-for-byte unchanged): DPR cap 2→1.5, default budget 2 M→1 M, `lodRenderScale` 0.75→1.0 (stop at the 1 px floor — no sub-pixel fill chase), `minPixelRadius` 1.5 (discard <1.5 px splats, Spark's top fill lever), throttled sort, and `maxPagedSplats` 16 M→12 M (covers the M1's ~115-chunk working set at the 1 M budget with headroom, ~250 MB lighter on M1's unified memory where the JS heap already peaked ~2 GB). Combined ≈4–6× fill reduction targeting smooth fps; pending real-MacBook validation. Also: the bare **Bench** button now runs the programmatic orbit instead of a static no-motion recording (users hit the meaningless-static trap on Mac *and* iPhone); `?bench=1` still forces static, and a new `?bench=dolly` plays a programmatic zoom-in (the trusted fine-detail-streaming harness used to verify the storm fix).

- **Near-field LoD starvation fixed — high-detail bubble locked to the camera (#81).** User repro (two stills, same pose, 1.5 M budget): the centre cone was razor-sharp but foreground foliage 1–2 m off the centre ray was base-level blur, and it snapped to sharp the instant the centre ray crossed it ("things jump around too much"). Root cause (verified against `spark/rust/spark-worker-rs/src/lod_tree.rs::compute_pixel_scale` + the template, *not* the agent's first patch which had the `pixel_scale = size·(1/distance)·foveate` arithmetic inverted): the auto view-tracking focus teleported the LoD origin (`spark.lodPosOverride`) to ~90 % of the screen-centre hit distance — i.e. onto the *far* looked-at surface. Near geometry off the centre ray was then ~tens of metres from `origin` (tiny pixel-scale) **and** outside the 55°/110° cone (×0.5 foveation); both stack → radial near-field collapse, and the per-frame teleport between near/far hits caused the LoD pop. Fix (`template.py`, `_autoFocusTick`, template-only — rcf2 fork unchanged): the virtual LoD origin (`spark.lodPosOverride`) is **pinned to the real camera** (`FOCUS_MAX_AHEAD` default **0**), so near geometry always gets finest LoD by true distance and never depends on where the centre ray lands. **Correction:** an interim default of `3` m-*ahead* of the camera was shipped and itself **regressed the foreground** — at a ground-truth user pose, foliage ~1 m in front had its LoD measured from a point 3 m *past* it → coarse → "foreground totally blurred"; reproduced on a desktop RTX at 120 fps (so not device-specific) and A/B-isolated at that exact pose (`?focusAhead=0` razor-sharp incl. bamboo leaves/bench slats; `=3` mush; `?coneFoveate=1` ruled foveation out). `0` keeps the centre sharp too (camera-distance LoD; verified same shot). Trade: at a *far aerial* framing the looked-at background is no longer pulled to extra-fine leaves (honest distance LoD) — accepted per the explicit priority "always high-detail ~5 m around the user, not base level". `?focusAhead=N` opts back into an ahead-bias (clamped 0–30 m) if ever wanted. Refetch-storm-safe (origin = camera ⇒ the camera-centred working set the 16 M desktop pool was already sized for).
- **Chromium-on-M1 streaming jitter — root cause isolated to intrinsic ANGLE-Metal upload; two fork attempts measured + reverted; `SPARK_FORK_URL` stays `rcf2`; architecture decision pending.** Measured on an M1 Pro, same scene/workload: Safari (native WebKit→Metal) 0 main-thread longtasks / p99 36 ms, but Chrome/Brave (ANGLE→Metal) **82–86 longtasks / p99 ~70 ms** during chunk streaming. Discriminator (user forced `--use-angle=gl` → ANGLE-OpenGL backend, same JS/V8): longtasks collapsed to **16** — the cause is the **ANGLE-Metal texture-upload path**, not V8-GC. Two fork fixes were tried and **both A/B-disproved by the user and reverted**: (`rcf3`) frame-budgeting `SplatPager.processUploads()` to ≤6 ms/frame — longtasks unchanged (86), jank *worse* (≈28→45); (`rcf4`) replacing the per-page `addLayerUpdate`+`needsUpdate` with a direct per-page `gl.texSubImage3D` (no per-page `needsUpdate`) — longtasks **still 85**, jank **49** (worst), p99 100 ms. `rcf4` objectively *proved* three.js no longer re-processed the texture (its `.version` went from per-page to **1**) yet longtasks did not move → **the longtask was never the three.js `needsUpdate` wrapper; it is ANGLE-Metal's synchronous *staged* texture upload itself** (`chrome://gpu`: `alwaysPreferStagedTextureUploads: Enabled`), intrinsic to that driver path regardless of who triggers it. Both attempts reverted to plain `rcf2` (`spark/src/SplatPager.ts` restored; `SPARK_FORK_URL` = `_sparkfork-rcf2`, RefCell + codebook gate, the canonical good build `IBUG_cs_v19`). Presented as an architecture decision; **user chose async PBO upload**, now shipped as **`rcf5`** (`SPARK_FORK_URL` → `_sparkfork-rcf5`, RefCell + codebook WASM re-inlined unchanged) for the user's M1 A/B. Basis (source-grounded research, ANGLE's own Metal code, not forum hearsay): ANGLE-Metal has a dedicated PBO **blit fast-path** (`TextureMtl.mm setPerSliceSubImage` → `MTLBlitCommandEncoder` GPU buffer→texture copy, async w.r.t. the main thread) that is a *different* code path from the synchronous staged one (`alwaysPreferStagedTextureUploads` governs only the non-PBO path); the "PBO is useless on ANGLE" claim is D3D11-only, not Metal; our RGBA32UI/RG32UI 256² offset-0 tightly-packed uploads keep the fast path. `SplatPager.uploadPage()` now streams each changed page layer through a **3-deep `PIXEL_UNPACK_BUFFER` ring** (`bufferData(null, STREAM_DRAW)` orphan → `bufferSubData` → `texSubImage3D` from the PBO at offset 0; round-robin so a reused slot is ~2 frames old → no fence; strict `pixelStorei` incl. `UNPACK_IMAGE_HEIGHT=0` per CVE-2025-14174; modelled on the proven live `indicesTexture` path so three's state cache stays synced and no `resetState` is needed). Self-pixel-verified clean on desktop (pixel-identical to v19, 0 aborts, PBO ring active, error queue clean) — but the user's M1 `?bench=rotate` A/B **disproved it and it was reverted**: longtasks **86 (unchanged)**, jank **56 (worst of all four attempts: 28→45→49→56)**, p99 103 ms — the documented path-gated risk hit (this M1's opaque ANGLE feature flags routed the PBO through the slow synchronous CPU map, the *worse* path). **Net of three fork attempts (`rcf3`/`rcf4`/`rcf5`): the ANGLE-Metal longtask count is rock-stable at ~86 and immune to *how* we upload — proven three independent ways. The cost is intrinsic to ANGLE-Metal processing this streaming upload volume.** All reverted to plain `rcf2` (`SPARK_FORK_URL` = `_sparkfork-rcf2`; `spark/src/SplatPager.ts` restored — 8 `addLayerUpdate`+`needsUpdate` sites back, helper + PBO fields removed, RefCell + codebook gate intact; canonical good build `IBUG_cs_v19`; orphan rcf3/4/5 bundles linger unreferenced on Bunny). Per the 3-strikes rule **no further blind fork attempt**. The remaining honest end-states, presented to the user for their effort/quality call: **(A) volume reduction on M1** (coalesce/throttle to far fewer, larger uploads/frame — the one lever attacking the actual proportionality longtasks∝streaming-work; backend-agnostic; real engineering but a genuine detail/streaming-speed tradeoff on M1 Chromium), or **(B) accept** it as the upstream ANGLE-Metal-on-macOS limitation (Safari native = 0 longtasks/smooth) — document it, optionally a gentle "Safari recommended on Mac" hint + a modest M1-only volume trim as the 80/20. Recommendation logged: B + modest trim, unless smooth-on-Chromium-M1 outranks M1 detail. **Resolved — B (accept).** The single remaining hypothesis (the prime-suspect Chromium launch flag `--disable-angle-features=alwaysPreferStagedTextureUploads`, the exact feature WebKit disables by default) was then **directly user-tested on the M1**: `?bench=rotate` with the flag active (ANGLE-Metal confirmed still the backend, the feature confirmed off) gave longtasks **83** — **unchanged**, still stuttery. That **disproves the single-flag hypothesis**: Safari's smoothness is its entire native-Metal pipeline, not one ANGLE toggle, and the synchronous staged-upload cost on Apple Silicon is deeper than any feature flag. With 3 fork upload rewrites + WebGPU + ANGLE's own off-switch all failing (~83–86 LT) and only Safari (no ANGLE) at 0, the investigation is **exhausted with no browser- or app-mechanism fix**. User decision: **ship B** — see the new "Safari-on-Mac streaming hint" Added entry below (the agreed user-facing mitigation; the optional upstream ANGLE bug report is deferred — "just do 1 for now"). No further fork attempts (3-strikes) or flag re-tests.
- **Bench rebuilt robust + resilient — camera-rotation & `t=null` fixed for good.** Three root causes (3 parallel root-cause agents + a malformed user trace): (1) `_benchTick` derived per-frame `t` from `_benchEndTime`, which is `Infinity` for every moving mode (cold/orbit/dolly/named) → `t = −Infinity` → JSON `null` (all path traces had null timestamps — not cold-specific). (2) `controls.update()` ran every frame *after* the path wrote the camera, re-deriving it from the OrbitControls spherical and forcing `lookAt(target)`; `controls.enabled=false` does **not** stop `update()` in three 0.180 (verified — no `enabled` guard in the pinned source), so the path write was clobbered. Orbit only survived by coincidence (keyframes lookAt the same target); dolly and any pre-bench damping residual broke silently → the recurring "bench runs but the camera doesn't rotate." (3) The new Apple-Silicon fps-cap early-return sat above the player advance, freezing the camera + dropping samples on capped frames. Fixes (`template.py`, surgical — no path/spline-internal changes): monotonic recorder clock (`_benchT0`; `t = now − _benchT0`) + accurate `durationS` at stop; **`controls.update()` gated on `!_player`** so a path/bench owns the camera unclobbered (resumes normally after); the fps-cap never skips while `_player`/`_benchActive`; a built-in **camera-motion self-check** that accumulates real `camera` world-travel during playback and at stop writes `trace.cameraMoved`/`cameraTravel`/`cameraNetDisplacement`, emits a loud `console.error` for moving modes that didn't move, and exposes `window.__benchResult` + a `bench:done` `CustomEvent` so a dead bench can never silently pass and Playwright can assert rotation deterministically; camera-halt now coincides with stop/download; `peakUsedMB` is `null` (not a misleading `0`) on Safari/Firefox; longtask timestamps share the frame clock. Verified on GRYZEN incl. a forced OrbitControls-clobber negative test (bench still passes) and a forced no-motion test (self-check fires).
- **Splat-budget dropdown now stays in sync with programmatic budget changes (#50).** The on-screen `<select id="splat-budget">` only reflected the user's own choices and the initial device-tier pick; any later code-driven change to `spark.lodSplatCount` (the Apple-Silicon profile, `?budget=`, future foveation/tiering) left a stale value on screen — confusing in the UI and misleading in screenshots/benches. `applySplatBudget()` is now the single source of truth: after setting `spark.lodSplatCount` it calls `selectClosestBudget()` to snap the dropdown to the nearest option ≤ the actual budget (skipped for n=0 "No limit", which has no matching option). Setting `.value` programmatically doesn't fire `change`, so there's no recursion with the user handler. UI/screenshot self-consistency holds no matter who changes the budget.

### Removed
- **`tests/test_html_for_save_mode.py::test_defaults_are_regression_safe_existing_scenes_byte_identical` and its 14 source-anchor constants** (`_PRE_TASK20_REMAINDER_*` + `_PRE_TASK20_FULL_*` and the provenance `_PRE_TASK20VC_*` / `_PRE_TASK19_*` / `_PRE_TASK17_*` / `_PRE_TASK16_*` / `_PRE_TASK15_*` / `_PRE_TASK14_*` chain, the `_T16_TRAJ_*` / `_T15_OB_*` / `_T14_PW_*` / `_T14_AF_*` / `_T13_LB_*` / `_T13_AS_*` / `_T13_IC_*` / `_T12_CSS_*` / `_T12_DOM_*` / `_T12_MM_*` / `_T19_CSS_*` / `_T19_JS_*` / `_T20_CAMSEL_DOM_*` / `_SPLINE_REGION_*` excise region anchors) and the `_excise()` helper. T6 of #118 retires the source-level excised-region byte-lock; the output-pin in `tests/test_html_for_output_pin.py` (6 generated-HTML fixtures vs the old 1-fixture excision) is the modularization-safe successor. The kwarg-shape tests, the 4 NEGATIVE-CONTROL tests (camera-path overlay / tick line / gizmo handle drag / 2026-05-20 author-mode UX), the SAVE_* anchor tests, and the publish/assembler call-site tests in `test_html_for_save_mode.py` all survive intact -- they check HTML content / call-site threading, not source structure.

## [0.7.0] - 2026-04-17

### Changed
- **Minimum Python is now 3.12** (was 3.11). Drops six `hasattr(path, 'is_junction')` polyfills across `core/project.py` and `web/routes/projects.py` — `Path.is_junction()` has been native since 3.12. CI matrix updated: drops 3.11, keeps 3.12 + 3.13, **adds Windows runner** (since junction handling is Windows-specific and now uses native API).

### Added
- **3DGS Render addon install guide** in `docs/dcc-bridge.md` for the Blender Tier-2 plugin — step-by-step on installing the optional [ReshotAI/gaussian-splatting-blender-addon](https://github.com/ReshotAI/gaussian-splatting-blender-addon) so the splat shows as continuous Gaussians instead of points while the user authors a camera against it. Includes a perf note (point preview while scrubbing, rendered for keyframe placement).
- Note in the Blender plugin's camera-extraction docs that the splat object is temporarily hidden during sampling (already implemented in v0.6.2; now documented).

## [0.6.3] - 2026-04-17

### Fixed
- **Max plugin Stand-Up Parent setup — wrong inner rotation in 3ds Max only.** In Max, assigning `inner.parent = outer` preserves the world transform, so setting `inner.rotation = +90°X` *before* parenting (the previous order) left `inner.world = R_+90X` instead of the documented composed `R_-90X`. Two visible symptoms: (a) the splat appeared upside-down in the Max viewport, (b) the v0.6.2 export math gave wrong PC coords (DCC `(0, 0, 5)` came back as PC `(0, -5, 0)` instead of `(0, 5, 0)`, mirrored on Y). The plugin now sets `inner.parent = outer` first, then `inner.rotation = (eulerangles 90 0 0)` — which Max interprets in local coordsys after parenting, yielding the correct `inner.world = R_-90X`. Blender unaffected (it composes `matrix_world = parent @ local` natively, so order doesn't matter there). Caught via 3dsmax-mcp Tier-1 round-trip test against a known-position keyframe sweep — Max output now matches Blender output to ≤1e-6 unit. Worked example in `docs/dcc-bridge.md`.

## [0.6.2] - 2026-04-17

### Fixed
- **DCC bridge math — Stand-Up Parent compose was missing the final 180°-X flip.** Cameras animated in 3ds Max or Blender came back mirrored on Y: a camera placed at DCC `(0, 0, 5)` (5 units above the splat) was returning as PC `(0, -5, 0)` (5 units *below*) instead of `(0, 5, 0)`. Caught by a Tier-1 Blender round-trip via `blender-mcp`. Both plugins (`tools/dcc-bridge/max/splatpipe_bridge.py`, `tools/dcc-bridge/blender/__init__.py`) and the in-editor Claude prompt template now compose `cam_in_pc_displayed = R_180X @ inv(inner.matrix_world) @ cam.matrix_world` (column-vec / Blender) or `cam.transform * inverse(inner.transform) * rotateXMatrix(180)` (row-vec / MaxScript). Why the extra `R_180X`: `inv(inner)` only takes a DCC world point into the splat's *local* (PLY-native, Y-down) frame; Splatpipe stores camera-paths in PC-displayed (Y-up) frame, which is `R_180X @ PLY-native`. `docs/dcc-bridge.md` updated with the corrected math, the worked example, and a regression note.
- **Blender camera sampling perf** — `bpy.context.scene.frame_set(f)` triggered a full depsgraph evaluation on the 3M-vertex Gaussian splat preview every frame, which on dense paths (240 frames @ 24 fps × 10s) wedged the MCP connection. The Send-camera operator now temporarily hides the splat object (`hide_viewport=True, hide_render=True`) for the sampling loop and restores its prior visibility after, dropping the per-frame cost to milliseconds.

## [0.6.1] - 2026-04-17

### Added
- **DCC bridge** for round-trip camera-path authoring in 3ds Max + Blender. Three new HTTP endpoints under `/projects/{p}/dcc/`:
  - `GET /manifest` — project metadata, splat URLs (full + preview LOD), available LODs, COLMAP detection, frame range, fps, existing paths.
  - `GET /splat.ply?lod=N` — Range-streamed reviewed PLY for the DCC viewport.
  - `POST /import-camera` — accepts JSON `{name, fps, coord_frame, frames: [...]}` or a multipart `.glb` upload; appends a new entry to `scene_config.camera_paths`.
- **Stand-Up Parent** coordinate-system contract documented in `docs/dcc-bridge.md`. Bridge clients wrap the splat in two nested empties (`splatpipe_outer` 180°-X + `splatpipe_inner` +90°-X = net -90°-X). Net effect: splat appears upright in the DCC's Z-up world; on export the camera is composed against `inv(inner.matrix_world)` to land in PlayCanvas-displayed frame. Importer accepts `coord_frame: "playcanvas_displayed"` (already-composed) or `"ply_native"` (applies the 180°-X flip on the way in).
- **3ds Max plugin** (`tools/dcc-bridge/max/splatpipe_bridge.py`) — pymxs script with a tkinter dialog: paste project URL → Pull splat (creates VRayGaussiansGeom + Stand-Up Parent + camera + frame range) → animate → Send camera. Pure stdlib, no extra deps.
- **Blender addon** (`tools/dcc-bridge/blender/__init__.py`) — `bl_info` block, two operators (`splatpipe.pull_splat`, `splatpipe.send_camera`), sidebar panel in 3D Viewport. Uses `bpy.ops.wm.ply_import` (Blender 4.0+); recommends optional [3DGS Render addon](https://github.com/ReshotAI/gaussian-splatting-blender-addon) for full Gaussian preview.
- **"Author in Max / Blender via Claude" button** in the scene editor — opens a modal with a copyable prompt template that drives the same workflow via the `3dsmax-mcp` (and optional community `blender-mcp`) MCP servers. No DCC plugin install required for the Tier 1 path.

### Notes
- Manual smoke tests of the plugin buttons (D4 / D5 / D8 in the original plan) are user-facing and require live Max + Blender installs with the corresponding MCP servers; not run by CI. The HTTP endpoints + the Stand-Up Parent math are end-to-end verified.

## [0.6.0] - 2026-04-17

### Added
- **Camera path tours** — author smooth fly-through cameras in the scene editor or import them. Per-path keyframes carry `pos`/`quat`/`fov`/`hold_s` plus optional `annotation_id` for highlighting. Per-path `smoothness` slider (0..1, default 1.0 = full Catmull-Rom) and `play_speed` log-scale slider (0.01×..5×) work live during playback.
- **Smooth-pass-through spline playback** — ported SuperSplat's MIT cubic Hermite (`src/anim/spline.ts`) into both viewers. 8-D spline over `(pos.xyz, quat.xyzw, fov)`, quat renormalised per frame; `fromPointsLooping` for smooth loop wrap. C1 (velocity) continuity through every keyframe — no stop-start at keys. Same algorithm in PlayCanvas + Spark, byte-identical motion.
- **glTF camera importer** — `splatpipe path-import scene.glb` (or web upload) parses `KHR_animations` channels for the first perspective camera, walks parent transforms, samples to fixed-step keyframes, applies the 180°-X PLY-native → PC-displayed flip, appends as a new path.
- **COLMAP capture-camera importer** — `splatpipe path-import-colmap` reads `01_colmap_source/cameras.{txt,bin}` + `images.{txt,bin}`, computes camera-in-world from each image's pose, applies the 180°-X flip, emits a path with one keyframe per image (or every-Nth). Friendly error for passthrough projects with no COLMAP source. Algorithm references SuperSplat's `colmap-loader.ts` (MIT).
- **Spark 2 renderer** — per-project `renderer: "playcanvas" | "spark"` toggle in the Assemble settings panel. PlayCanvas remains default; switching to Spark emits a `.rad` streaming LoD viewer.
  - `splatpipe build-lod <project>` standalone CLI for warming the cache.
  - `viewers/spark/build_lod.py` wraps the Rust `build-lod` binary (sparkjsdev/spark MIT) — toolchain detection (cached binary → `$SPARK_REPO` release → `cargo run --manifest-path …` first-run), sha256+rev cache key in `~/.cache/splatpipe/rad/`, subprocess streaming with `ProgressEvent`-style callback, atomic move into the cache.
  - `viewers/spark/template.py` emits a self-contained THREE.js + `@sparkjsdev/spark@2.0.0` viewer pinned to `three@0.180.0` (peer dep). Loads `scene.rad` with `paged: true` for HTTP-Range streaming. CSS2DRenderer annotation markers, OrbitControls, conditional bounds clamp, conditional `THREE.AudioListener` only when audio sources exist, foveation knobs (`coneFov0`, `coneFov`, `coneFoveate`, `behindFoveate`).
  - Tone mapping mapped from PC names: `neutral`→Neutral, `aces`/`aces2`→ACESFilmic, `filmic`→AgX, `linear`→Linear.
  - Splat oriented with the same 180°-X flip as PC so annotations + camera-paths land in the same place across renderers.
- **Annotation `id` migration** — `Project.__init__` now runs `_migrate_state()` once, idempotently backfilling stable `id`s on existing annotations so `keyframe.annotation_id` references stay valid.
- **Cascade on annotation delete** — deleting an annotation clears any `keyframe.annotation_id` references to it across all paths.

### Changed
- `DEFAULT_SCENE_CONFIG` extended with `camera_paths`, `default_path_id`, `spark_render` (lod_splat_scale, lod_render_scale, foveation, ondemand_lod_fallback).
- New `pygltflib` runtime dependency (camera-path glTF import).
- `LodAssemblyStep.run() / run_streaming()` dispatch on `project.renderer`. PlayCanvas pipeline path (chunked SOG via `splat-transform`) is unchanged; Spark path delegates to the new `SparkAssembler`.
- `splatpipe assemble` summary print handles both renderer shapes (PlayCanvas: chunk count; Spark: `.rad` size, optional `.sog` fallback flag).

### Fixed
- Mid-drag DOM rebuild bug on per-path sliders — `updatePathSilent` saves to the server without triggering `loadPaths()`/re-render so the slider keeps its dragged value.
- `gotoKeyframe` in the scene editor — direct `camera.setPosition` was overridden by PlayCanvas's `CameraControls` on the next update tick. Now uses `cc.reset(focus, position)` (`camera-controls.mjs:697`) so the new pose sticks AND the user can keep orbiting from there. `stopPath` rebinds the controls the same way so playback ending doesn't snap back.

### Notes
- Spark output requires the Rust toolchain + a sibling clone of [sparkjsdev/spark](https://github.com/sparkjsdev/spark) (or `SPARK_REPO` env var). First-time `cargo build --release` of the workspace takes ~2 min; subsequent runs use the cached `build-lod` binary.
- PlayCanvas pin remains `2.17.0` (2.17.1+ has a regression — see 0.5.0 notes). Spark pin is `2.0.0`.

## [0.5.0] - 2026-04-16

### Security
- Bumped minimum `python-multipart` to 0.0.22 (CVE-2026-24486)
- Bumped minimum `pytest` to 9.0.3 (CVE-2025-71176)

### Fixed
- **LichtFeld Studio no longer risks a buffer-deadlock hang**: the trainer was opening `stderr=subprocess.PIPE` and only reading it after the stdout loop, so a LichtFeld run that emitted more than ~64 KB of stderr could block the subprocess indefinitely. Merged stderr into stdout (same pattern the Postshot trainer already uses); the stderr tail is still surfaced on failure via `TrainResult.stderr`.
- **`state.json` corruption surfaces a readable error instead of a JSONDecodeError traceback**: `Project._load_state` now raises `RuntimeError("state.json is corrupted at ...")` so the runner's failure path records something the user can act on.
- **`state.json` writes are now atomic**: `Project._save_state` writes to `state.json.tmp` and then `os.replace`s it into place, so a crash or kill mid-write no longer leaves a truncated state file behind.
- **Passthrough cancel takes effect during extraction, not after**: `PassthroughTrainer` no longer blocks the generator with `Popen.communicate()` during `.psht` export. It now uses a reader thread + 2s heartbeat yields (same pattern as `PostshotTrainer`), so cancel clicks kill the process within a couple of seconds instead of waiting for the full export to finish.
- **Manual review approval is preserved under a tight race with the runner**: `_execute_review` now re-reads state one more time right before recording `waiting`, so an approval that lands between the initial check and the write short-circuits cleanly instead of getting clobbered.
- **Camera Constraints toggle now actually turns off**: the `Enabled` checkbox was missing a hidden `enabled=false` sibling input, so unchecking sent nothing and the merge preserved the previous `enabled=true`. Added the hidden input; also added regression tests for both the on and off transitions.
- **Passthrough review step preserves existing completed summary**: when a user had already approved the review step (manually or in a prior run), `_execute_review` now short-circuits to the "already approved" branch before the passthrough auto-approve branch, so prior summary data isn't clobbered.
- **Viewer no longer hangs on PlayCanvas 2.17.x patch releases**: pinned the viewer's PlayCanvas import to `2.17.0` (was `2.17`, which floated to 2.17.2). Patches 2.17.1 and 2.17.2 introduced a regression that fired `Cannot read properties of undefined (reading 'listener')` every frame in the engine update loop and prevented splat rendering (splat count stuck at 0.00M, loading spinner never hides). Camera-controls.mjs is now version-pinned the same way to keep them in lockstep.
- `audiolistener` component is now only attached when `viewer-config.json` actually has audio sources — saves the engine some idle work in the common no-audio case.

### Added
- **Camera constraints toggle**: new `Enabled` checkbox in the Camera Constraints panel. Default is OFF — the viewer lets the user fly anywhere with no pitch/zoom/ground/bounds clamping. Turning it on applies the existing constraint values. Fixed a related state bug where partial scene-config saves (e.g. one number field) wiped sibling fields; `Project.set_scene_config_section()` now merges dict updates instead of replacing them.
- **Passthrough trainer**: publish a finished `.psht` or `.ply` to Bunny CDN without retraining. Selected from the trainer dropdown — extracts the embedded splat (`.psht` via `postshot-cli export`) or copies the file (`.ply`), then runs assemble + export. Auto-skips `clean` and `review` steps.
- **Standalone `.ply` source format**: `splatpipe init scene.ply` and the web "+ New Project" form now accept raw `.ply` files. Auto-defaults to passthrough trainer.
- **Smart trainer default**: projects created from `.psht` or `.ply` sources auto-select the passthrough trainer; COLMAP sources still default to postshot.
- **Step info tooltips**: every pipeline step in the project detail view now shows a hover info icon explaining what the step does.
- **Passthrough mode banner** on the project detail page when trainer is passthrough, with a one-line summary of what runs.
- Postshot 1.0.331 `--pose-quality` support (1=Fast, 4=Best, default 3)
- Postshot 1.0.287 support: Splat ADC/MCMC profiles, GPU selection, SH degree control, no-recenter, image selection mode
- LichtFeld Studio v0.5.1 support: PPISP per-camera appearance modeling (`--ppisp`, `--ppisp-controller`)
- LichtFeld headless training mode (`--headless --train` flags, always enabled for CLI/pipeline)
- LichtFeld quality options: SH degree, mip filter (anti-aliasing), bilateral grid, image downscaling, tile mode for VRAM management
- LichtFeld sparsity optimization and undistort support
- New config options for both trainers exposed in web settings page

### Changed
- LichtFeld CLI command now always includes `--headless` and `--train` for pipeline use
- Bumped `@playcanvas/splat-transform` to ^1.10.2 (from ^1.7.0)
- Bumped PlayCanvas CDN from 2.16 to 2.17 (viewer, scene_editor, and assembled LOD viewer)
- Migrated LOD distance API from `lodDistances` array to `lodBaseDistance` + `lodMultiplier` (geometric progression; required by PlayCanvas 2.17)
- Replaced per-LOD distance sliders in assembled viewer with base + multiplier sliders plus live per-LOD distance preview

### Removed
- "Headless mode uncertain" caveat from LichtFeld known limitations

## [0.4.1] - 2026-02-15

### Fixed
- Unused imports flagged by ruff lint in CI

## [0.4.0] - 2026-02-15

### Added
- Postshot `.psht` file as direct input source: create projects from `.psht` files, Splatpipe copies the file and trains each LOD from it
- Splat count warning: after each LOD training, compares exported PLY vertex count to target and warns if over budget
- Review re-export: checkbox on "Approve & Continue" to re-export PLYs from edited `.psht` files via `postshot-cli export`
- `detect_source_type()` in parsers: handles both files (`.psht`) and directories (COLMAP, Bundler, etc.)
- `source_type` and `source_file()` on Project model for .psht source tracking
- CLI `splatpipe init` accepts `.psht` files as input (copies to project, never modifies original)
- CLI `splatpipe train` handles `.psht` source projects
- Scene authoring: `viewer-config.json` carries camera constraints, splat budget, and future scene settings from dashboard to viewer
- Camera constraints in production viewer: configurable pitch limits, zoom range, ground height, and bounds radius
- Per-project default splat budget: set initial budget in dashboard, viewer reads from config at startup
- Custom LOD distance sliders: color-coded sliders matching PlayCanvas engine's colorizeLod debug view
- Dashboard UI: Camera Constraints and Default Splat Budget controls in Assemble settings
- `splatpipe serve` auto-generates `viewer-config.json` fallback if missing
- Assembly copies project `assets/` folder to output (for future audio/media support)
- Scene editor: visual click-to-place annotation tool with full 3D viewer, annotation sidebar, and ray-based ground-plane placement
- Annotation markers in production and preview viewers: numbered orange pins with hover tooltips, projected from 3D world positions
- Annotation CRUD routes: add, update, delete annotations with automatic re-labeling
- Audio support in viewer: ambient and positional audio from `viewer-config.json` with PlayCanvas SoundComponent
- Audio management in dashboard: upload audio files, configure volume/loop/positional per track
- Audio CRUD routes: add, update, delete audio sources with file upload to project assets
- Global pipeline queue: enqueue runs for multiple projects from the dashboard, see pending jobs, reorder, pause, and cancel
- Queue panel on projects page with live progress for the current job and management controls for pending jobs
- Background color setting in dashboard and viewer: configurable clear color applied to canvas and page body
- Post-processing settings: tonemapping (linear/neutral/aces/aces2/filmic), exposure, bloom, and vignette controls in dashboard
- Multi-format alignment import: detect COLMAP (.txt/.bin), Bundler, RealityScan, and BlocksExchange formats
- Binary COLMAP support: clean step auto-converts `.bin` files to text before filtering
- Format detection on project creation: shows detected format, never blocks (unknown formats allowed)

### Changed
- "COLMAP Source" label renamed to "Source (Postshot, COLMAP, etc.)" in dashboard
- "Alignment Data Directory" renamed to "Source (Postshot file, COLMAP folder, etc.)" on project creation form
- Browse buttons split into "Folder" and "File" for source path selection
- Clean step auto-skips for `.psht` input projects (no COLMAP data to clean)
- Project creation form simplified: removed Training and Pipeline Steps sections (configurable on project detail page after creation)
- `splatpipe init` warns on unknown alignment format instead of blocking
- `splatpipe clean` accepts binary COLMAP input (converts to text internally)

### Fixed
- PLY header reader no longer hangs on invalid/truncated PLY files (infinite loop on missing `end_header`)

## [0.3.1] - 2026-02-14

### Changed
- SH bands default changed from 0 (no SH) to 3 (full SH) for better visual quality out of the box

## [0.3.0] - 2026-02-14

### Added
- SH bands setting: configurable spherical harmonics filter (0–3) on Assemble step, defaults to 0 (no SH, smallest files)
- Inline LOD splat count editing in Train step settings (accepts M/K format)

### Changed
- LOD folder names simplified from `lod0_20000k` to `lod0` (decoupled from splat count)
- Default LOD0 bumped from 20M to 25M for new projects

### Fixed
- LOD renumbering now preserves per-LOD settings (enabled, train steps)

## [0.2.0] - 2026-02-13

### Added
- CDN folder name setting: configurable remote path for Bunny CDN exports (defaults to project name)
- CDN model browser: "Refresh" button lists existing folders on Bunny CDN, click to select
- Local preview: serve `05_output/` files through the dashboard for HTTP-based PlayCanvas viewer preview
- Splat budget dropdown in viewer (No limit, 1M, 2M, 3M, 4M, 6M) — defaults to 4M desktop, 1M mobile
- Bunny CDN credentials UI: editable on Settings page, status indicator on Export step panel
- Project history log: all step runs recorded with timestamp, duration, and summary (collapsible section on project detail page)
- Persistent export URL: CDN viewer/folder links survive page reload on Export step card

### Changed
- Viewer HTML now uses PlayCanvas engine with LOD streaming instead of SuperSplat iframe embed
- Viewer supports fly + orbit camera controls (WASD + mouse) and respects project LOD distances
- COLMAP clean step disabled by default for new projects

## [0.1.5] - 2026-02-13

### Fixed
- Cross-platform test fix: `test_open_folder_nonexistent` used Windows-only path that resolved differently on Linux CI

## [0.1.4] - 2026-02-13

### Fixed
- Python 3.11 compatibility: guarded all `Path.is_junction()` calls (added in 3.12)
- Added `python-multipart` to web dependencies (required by FastAPI for form parsing)

## [0.1.3] - 2026-02-13

### Fixed
- Added `httpx` to dev dependencies (required by `starlette.testclient` but not pulled in automatically)
- Removed COLMAP from README requirements table (Splatpipe reads COLMAP output, doesn't require COLMAP installed)

## [0.1.2] - 2026-02-13

### Fixed
- CI workflow now installs web dependencies (`.[dev,web]`) so web route tests pass on GitHub Actions

### Removed
- Unused logo variants (`SplatPipe_Logo_01.png`, `SplatPipe_Logo_02.png`)

## [0.1.1] - 2026-02-13

### Removed
- COLMAP tool path setting and GUI launch button (Splatpipe reads COLMAP output files but never calls the COLMAP executable)

## [0.1.0] - 2026-02-13

### Added
- CLI-first pipeline: `init`, `clean`, `train`, `assemble`, `export`, `serve`, `status`, `run`
- Pluggable trainer backends: Postshot and LichtFeld Studio with shared `train_lod()` generator protocol
- COLMAP streaming parsers for multi-GB `cameras.txt`, `images.txt`, `points3D.txt`
- COLMAP cleaning: camera outlier removal, KD-tree point filtering, POINTS2D reference cleanup
- LOD assembly via `splat-transform` producing PlayCanvas SOG streaming format
- Export to local folder or Bunny CDN (parallel uploads with progress)
- Web dashboard with FastAPI + HTMX + DaisyUI (project list, detail, settings, first-run wizard)
- Background pipeline runner: daemon-thread execution, SSE progress streaming, cancel support
- Manual review step: human gate between training and assembly with PLY/PSHT inspection
- Auto-generated `index.html` viewer using SuperSplat iframe (works on any hosting origin)
- File/folder browser dialogs for all path input fields
- Per-project config overrides via `project.toml` (deep-merged over `defaults.toml`)
- Debug JSON output with full command, stdout/stderr, timing, and environment for every step
- CI pipeline: ruff lint + pytest across Python 3.11/3.12/3.13

### Fixed
- Sequential LOD indices for splat-transform (sparse indices created empty LOD gaps on CDN)
- LOD directory cleanup before training (stale `.psht` caused duplicate radiance fields)
- Windows path normalization for runner lookup (URL forward-slash vs `Path()` backslash)
- Postshot progress parser matched real v1.0.185 output format

[Unreleased]: https://github.com/Geddart/Splatpipe/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/Geddart/Splatpipe/compare/v0.4.1...v0.5.0
[0.4.1]: https://github.com/Geddart/Splatpipe/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/Geddart/Splatpipe/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/Geddart/Splatpipe/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/Geddart/Splatpipe/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/Geddart/Splatpipe/compare/v0.1.5...v0.2.0
[0.1.5]: https://github.com/Geddart/Splatpipe/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/Geddart/Splatpipe/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/Geddart/Splatpipe/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/Geddart/Splatpipe/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/Geddart/Splatpipe/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/Geddart/Splatpipe/releases/tag/v0.1.0
